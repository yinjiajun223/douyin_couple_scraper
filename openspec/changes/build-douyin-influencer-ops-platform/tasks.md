## 1. 工程基础与兼容基线

- [x] 1.1 记录现有 `scraper.cjs`、`run.ps1` 和样例输出的可复现行为，将其保留为 legacy 入口，并通过一次离线解析回归确认重构前基线未丢失
- [x] 1.2 将仓库改为 npm workspaces，建立 `apps/web`、`apps/api`、`apps/worker`、`apps/collector`、`packages/contracts`、`packages/domain` 和 `packages/platform-douyin`，并验证全仓安装和构建成功
- [x] 1.3 配置统一 TypeScript、格式化、静态检查和测试命令，并验证根目录可以一次运行 lint、typecheck 和 unit tests
- [x] 1.4 实现服务端、worker 和 collector 的分层环境变量校验与脱敏错误输出，并用缺少必填项和包含占位密钥的测试验证启动会安全失败
- [x] 1.5 定义 API、collector、规则和 AI 结果的版本化共享契约，并通过契约序列化与不兼容版本测试

## 2. MySQL 数据模型与领域持久化

- [x] 2.1 建立 MySQL 8.0 迁移框架和迁移版本表，并在空数据库上验证首次迁移及重复执行均成功
- [x] 2.2 添加 workspace、用户、邀请、成员、会话、设备和审计表及约束，并用数据库集成测试验证邀请唯一性、角色关系和级联边界
- [x] 2.3 添加任务模板、筛选任务、不可变规则版本、采集运行和运行设备表，并验证历史运行引用的规则版本不可被更新
- [x] 2.4 添加达人、达人观察、作品、作品观察、运行来源和素材对象表，并验证 `(platform, platform_object_id)` 去重及观察快照只追加不覆盖
- [x] 2.5 添加候选、规则评估、AI 分析、人工复核、合作记录、备注和候选事件表，并验证一个达人可被多个任务引用且各自状态独立
- [x] 2.6 添加 AI 连接、后台任务和幂等键表，包含租约、重试和死信字段，并验证唯一键可阻止重复最终结果
- [x] 2.7 实现单工作区、三种角色和默认筛选模板的幂等种子流程，并验证默认值为粉丝 0-5000、近 15 天万赞、疑似 18-24 岁、素人、内容不限
- [x] 2.8 为关键列表和 worker 领取路径添加索引并运行查询计划检查，确认候选筛选和待执行任务查询不发生无界全表扫描

## 3. 内部账户、权限与设备授权

- [x] 3.1 实现首位管理员 bootstrap 命令和 Argon2id 密码存储，并通过重复 bootstrap、弱输入和密码校验测试
- [x] 3.2 实现登录、退出、会话撤销和账户停用，使用 Secure/HttpOnly/SameSite Cookie 与 CSRF 防护，并通过认证集成测试
- [x] 3.3 实现一次性、可过期邀请的创建与接受流程且不提供公开注册路由，并验证过期、重复使用和未邀请注册均被拒绝
- [x] 3.4 在服务层实现管理员、运营人员和只读成员授权矩阵，并通过逐角色 API 测试证明只读成员无法写入、运营人员无法管理系统密钥
- [x] 3.5 实现短期配对码、一次显示的设备令牌、令牌哈希、轮换和撤销，并验证设备令牌只能访问 collector 范围接口
- [x] 3.6 实现不可由普通成员修改的审计写入与查询，并验证账户、规则、设备、复核、合作、AI 设置和导出操作均产生脱敏审计记录
- [x] 3.7 完成登录、邀请、成员和设备管理页面，并用浏览器端到端测试验证管理员、运营和只读用户看到的操作不同

## 4. 可配置筛选任务与运行

- [x] 4.1 实现带 schema version 的硬筛、AI 和人工规则 JSON 校验器，并用合法、未知类型、越界阈值和旧版本样例验证
- [x] 4.2 实现任务模板和筛选任务的创建、复制、编辑、归档 API，并验证复制后修改不会改变来源任务或模板
- [x] 4.3 实现任务编辑页面，支持推荐画像说明、三类规则、AI 上限及作品数/时长/候选数停止条件，并用端到端测试验证保存回显
- [x] 4.4 实现运行创建时的规则快照与状态机，并验证运行开始后编辑任务只影响下一次运行
- [x] 4.5 实现运行领取、显式本地开始、暂停、继续、终止和停止条件判定，并用 API 状态转换测试拒绝非法跳转
- [x] 4.6 实现 `pass`、`fail`、`unknown` 三态硬筛引擎和证据引用，并用缺失粉丝数、窗口边界时间、点赞缩写及命中多作品测试证明未知值不按 0 处理
- [x] 4.7 完成任务列表、运行详情和实时进度页面，并以模拟 collector 数据验证数量、停止原因、设备和错误状态可见

## 5. Collector 同步与达人库

- [x] 5.1 实现版本校验、设备认证、运行归属和批量观察 ingestion API，并通过含 Cookie/密码字段、越权运行和不兼容版本测试
- [x] 5.2 实现客户端幂等键和逐条确认结果，验证同一批次重复提交不会重复创建达人、作品、观察或候选关系
- [x] 5.3 实现达人与作品规范化、跨任务去重和多来源关联，并通过两个设备发现同一账号的集成测试保留两条来源
- [x] 5.4 在 ingestion 事务后执行硬筛并创建候选、规则评估和命中作品证据，验证失败、通过和未知三种候选路径
- [x] 5.5 实现达人列表的任务、时间、粉丝、规则结论、人工状态、合作状态、负责人和标签组合过滤，并用查询集成测试验证交集语义
- [x] 5.6 实现达人详情页的观察时间线、任务来源、命中作品和规则快照展示，并用历史粉丝变化数据验证旧结论证据仍可查看
- [x] 5.7 实现任务归档和安全关联删除策略，并验证共享达人、仍被引用的观察、素材与审计不会被误删

## 6. 私有 OSS 素材链路

- [x] 6.1 实现 OSS 客户端封装和对象命名策略，禁止在 key 中使用用户名、文案或密钥，并用单元测试验证路径和 MIME/大小限制
- [x] 6.2 实现受设备和运行范围约束的短期签名 PUT 接口，并通过越权对象路径、过期签名和超限图片测试
- [x] 6.3 实现上传确认接口，校验对象存在、校验和、大小及观察关联，并验证未确认或不匹配对象不能成为业务证据
- [x] 6.4 实现经成员权限检查的短期签名 GET 接口，并验证未登录、跨工作区和已撤销成员无法取得素材地址
- [x] 6.5 实现未确认对象清理与可配置保留策略任务，并在测试 bucket 或 OSS mock 中验证仍被引用对象不会删除

## 7. Windows 抖音本地采集助手

- [x] 7.1 将现有数字解析、链接标准化和作者信息提取迁入 Douyin adapter，并用现有样例及新增 DOM fixtures 建立回归测试
- [x] 7.2 实现独立持久 Chrome 画像的创建、选择和复用，确保不读取系统日常浏览器目录，并通过两次启动验证登录资料目录保持一致
- [x] 7.3 实现本地配对和令牌安全存储，确保日志、错误页面和同步载荷不出现完整令牌，并通过脱敏测试和服务端撤销测试
- [x] 7.4 实现本地控制页的任务同步、画像选择、人工开始、暂停、继续、终止和进度显示，并用浏览器端到端测试验证服务器不能远程自动启动浏览
- [x] 7.5 实现可见推荐流的低速滚动、作品识别和重复作品幂等处理，并以静态 fixture/测试页面验证不调用点赞、关注、评论、私信、转发或“不感兴趣”动作
- [x] 7.6 实现潜在命中作者的公开主页核验，采集粉丝数和观察窗口内作品的时间、点赞和链接，并用窗口边界与未知字段 fixtures 验证结果
- [x] 7.7 实现登录失效、验证码、异常页面和低解析置信度的 fail-closed 检测，验证出现任一情况时保存进度、暂停并显示人工处理提示
- [x] 7.8 实现本地持久待同步队列、批量上传、逐条确认和断网重试，并通过中途断连与进程重启测试证明无丢失和无重复
- [x] 7.9 接入 OSS 签名直传截图和上传确认，验证图片流量不经过 API 主机且默认路径不存在完整视频上传
- [x] 7.10 实现采集器/协议/解析器版本上报和升级阻断页，并验证低于服务端最低版本时不会继续提交数据
- [x] 7.11 生成包含 Node 运行时、启动器和升级说明的版本化 Windows 分发包，并在干净 Windows 用户环境完成启动、配对和可见 Chrome 冒烟测试

## 8. AI 辅助筛选与后台任务

- [x] 8.1 定义 AI provider 接口及首个 OpenAI-compatible 多模态适配器，并用 mock 服务验证自定义 base URL、模型、鉴权和超时行为
- [x] 8.2 实现 AI 连接的服务端加密存储、密钥轮换和脱敏读取，并验证数据库、API 响应、前端和普通日志均不出现明文密钥
- [x] 8.3 实现管理员连接测试，分别检查文字、图片和结构化 JSON 能力，并用部分能力失败的 mock 响应验证诊断结果准确
- [x] 8.4 实现版本化提示词与输出 schema，覆盖疑似年龄、素人属性、内容标签、推广适配、风险、依据和逐项可信度，并通过不确定与非法输出测试
- [x] 8.5 实现 MySQL 后台任务领取、租约恢复、指数重试、去重和死信处理，并通过 worker 中途退出与双 worker 竞争测试
- [x] 8.6 在硬筛通过后按每次运行的人数、并发和预算上限调度 AI，验证硬筛失败和达到上限的候选不会产生新调用
- [x] 8.7 构造最小化 AI 输入，只包含任务规则、公开简介/文案和选定截图，并用请求快照测试证明不含 Cookie、设备令牌、联系方式、内部备注或完整视频
- [x] 8.8 持久化模型、提示版本、输入证据、结构化输出和错误状态，支持授权成员重新分析且保留旧版本，并用历史比较测试验证
- [x] 8.9 完成候选详情中的 AI 建议、依据、可信度、失败状态和重试界面，并用端到端测试证明 AI 失败不阻塞人工复核

## 9. 人工复核与合作跟进

- [x] 9.1 实现人工通过、不符合、待定及理由的领域操作，保留 AI 原结论与证据，并用人工覆盖 AI 的集成测试验证最终业务结论
- [x] 9.2 实现待复核、不符合、待联系、已联系、沟通中、已合作和不合作状态转换及历史事件，并用允许与禁止的转换表测试
- [x] 9.3 实现负责人、联系方式、报价、下一步和追加式备注的权限控制，并验证只读成员不能修改、授权成员修改后写入事件和审计
- [x] 9.4 对复核与合作记录实现乐观版本控制，并通过两个并发请求验证旧版本得到冲突响应而非覆盖新数据
- [x] 9.5 完成候选复核和合作跟进页面，展示规则、作品、私有截图、AI、历史与当前负责人，并用多人角色端到端场景验证
- [x] 9.6 实现按当前授权和过滤条件导出 CSV，排除会话、设备、AI 与 OSS 永久凭据，并通过字段白名单、内容转义和导出审计测试

## 10. 管理界面与可用性收口

- [x] 10.1 建立面向运营流程的导航与仪表盘，展示运行中任务、待复核、待联系、我的负责人项和失败任务，并用种子数据验证各计数可跳转到对应过滤结果
- [x] 10.2 为加载、空数据、权限不足、版本冲突、AI 不可用、采集暂停和网络失败提供明确中文状态，并通过组件与端到端测试覆盖这些分支
- [x] 10.3 完成键盘操作、表单标签、焦点反馈、颜色对比和移动宽度基本适配，并通过自动可访问性检查及人工窄屏检查
- [x] 10.4 在成员、设备、AI、模板和审计设置页隐藏无权限入口且保留服务端拒绝，并用三角色导航端到端测试

## 11. 服务器部署、安全与运维

- [x] 11.1 为 web 构建、API 和 worker 创建多阶段容器镜像及非 root 运行配置，并验证镜像扫描、容器用户和生产构建
- [x] 11.2 创建包含 TLS 反向代理、API、worker、静态 web、资源限制和持久日志策略的 Docker Compose 配置，并在本地或测试主机通过健康检查
- [x] 11.3 配置仅开放 80/443、HTTPS 跳转、请求大小和基础速率限制、安全响应头及真实客户端 IP，并用自动化 HTTP 检查验证
- [x] 11.4 编写生产环境模板和部署前检查，验证 RDS TLS、最小权限账号、OSS 私有 bucket、密钥强度及 RDS 不允许 `0.0.0.0/0` 白名单
- [x] 11.5 实现 liveness、readiness、结构化脱敏日志和 request/run/device/job/candidate 关联 ID，并模拟 MySQL、OSS 和 AI 故障验证状态区分
- [x] 11.6 提供受控迁移、滚动发布和前一镜像回滚脚本或操作手册，并在 staging 演练一次向前迁移和兼容回滚
- [x] 11.7 编写 RDS 备份、OSS 版本/生命周期和隔离环境恢复手册，并完成一次能对应恢复业务记录与素材引用的演练记录
- [ ] 11.8 在现有 Ubuntu 2核4GB 主机上做低并发负载测试，验证浏览器不在服务器运行、截图直传 OSS、内存/CPU/队列深度在记录的接受阈值内

## 12. 端到端验收与迁移

- [ ] 12.1 建立从创建任务、配对设备、推荐流采集、主页核验、OSS 证据、硬筛、AI 到人工复核的完整测试场景，并验证默认筛选模板的全部证据链
  - 2026-09-16 复核：本地浏览器验收已接入真实 API/MySQL、受控抖音页面与 OSS 替身；AI worker/厂商仍未纳入同一个完整场景，不能以分模块测试代替完整验收，本项恢复为待完成。
- [x] 12.2 建立验证码暂停、断网重试、重复 ingestion、AI 限流、worker 重启和并发复核的故障场景，并验证数据不丢失、不重复且可人工继续
- [x] 12.3 运行账户与数据边界安全测试，验证未邀请注册、越权修改、跨工作区读取、撤销设备、私有素材和秘密脱敏要求
- [x] 12.4 实现可选 legacy JSON/CSV 导入工具，将旧结果标记为 legacy run、缺失字段保持 unknown，并用当前仓库样例验证导入数量和去重报告
- [ ] 12.5 在 staging 用一个真实人工登录的抖音画像进行小范围试运行，抽样与人工页面结果对照并记录解析准确性、暂停行为和平台风险观察
- [ ] 12.6 完成运营、管理员和运维使用手册，覆盖人工养号边界、任务配置、设备配对、复核、AI、部署、备份和故障处理，并按手册由非开发同事走通一次验收
- [x] 12.7 达到 collector parity 后将 legacy 脚本从默认入口移至明确的兼容路径，保留可回退版本，并验证默认启动路径只进入新平台流程

## 13. 本地流程与页面交互修复（2026-09-16）

- [x] 13.1 在任务卡片提供创建运行入口，清楚区分配置任务、创建运行与本机人工开始，并提供下一步跳转
- [x] 13.2 补全本机配对、画像选择、人工打开抖音登录与运行状态界面，保留只允许本机显式开始的边界
- [x] 13.3 将实际推荐流、主页核验、硬筛、持久批次与截图签名直传串入运行；作品窗口不完整且未命中时保留 unknown
- [x] 13.4 用真实本地 API/MySQL 与受控页面/OSS 验证断网重试、截图失败、协调器重建和人工继续；断言候选、观察和已确认素材均不重复
- [x] 13.5 修复刷新/新标签页丢失 CSRF 的写入失败，增加设备撤销、列表刷新、详情请求竞态及错误反馈
- [x] 13.6 将负责人选择改为成员下拉框，验证人工复核、保存联系资料与推进合作阶段的完整浏览器操作
- [x] 13.7 收紧桌面信息层级与间距，统一主操作/表单反馈，复查窄屏布局与可访问性，并通过页面回归
- [x] 13.8 更新本地开发、采集助手和运营手册，明确 OSS 占位配置边界、恢复方法及真实抖音/AI/服务器验收尚未完成
- [x] 13.9 调整共享达人库可见范围：主库仅展示硬筛通过，未知进入“待补证据”，失败仅保留历史证据且不计入运营统计；增加领域与页面回归测试
- [x] 13.10 保持解析可信度阈值不变，在本地采集助手增加“连续 N 条低可信度后暂停/永不因低可信度暂停”配置；低可信度页面只记录本地诊断且不入库，成功解析后清零连续计数，并验证登录、验证码和异常页面仍立即暂停
- [x] 13.11 修复“永不因低可信度暂停”仍被连续未识别新作品/作者保护逻辑暂停的问题，将无新内容视为可跳过诊断并继续运行，同时验证目标停止、人工终止、登录失效、验证码和明确异常页面仍遵守各自停止或暂停规则
- [x] 13.12 修复推荐流耗尽可见作品后持续空转的问题：滚动实际内容容器，在永不暂停策略下按有上限的渐进退避安全重载当前抖音推荐页；仅发现新作品时清零无进展计数，并验证重复作品不重写、任务停止和安全暂停边界不变
- [x] 13.13 修复抖音新版作者主页中作品链接可见但点赞数和发布时间全部为空的问题：采集并校验主页公开作品列表响应，将 `digg_count/create_time` 与 DOM 作品按稳定标识合并，接口缺失时保留 DOM 兜底和 unknown 语义；用真实问题形态的 fixture 验证近 15 天万赞作品能够通过硬筛
- [x] 13.14 将作者主页核验固定为单一可见标签页：同一运行及后续运行复用该页、切换作者时只导航 URL 并自动置前，仅在用户手动关闭后重建；验证连续作者不会重复创建或关闭标签页，推荐流采集与安全暂停边界不变
- [x] 13.15 为达人详情的私有证据截图增加页面内大图预览、滚轮及按钮缩放、还原和键盘/遮罩关闭；复用现有短期签名地址，并通过页面回归验证不生成永久 OSS 地址且未授权素材仍不可访问

## 14. 达人库成员隔离与日期组织

- [x] 14.1 建立基于设备所有者、采集来源和明确负责人分配的达人可见范围查询，管理员拥有工作区全量范围，运营和只读成员仅拥有“自己采集或分配给自己”的范围；用两个运营采集同一达人及跨成员猜测 ID 的领域/API 测试验证
- [x] 14.2 将记录级范围应用到达人统计、列表、详情、导出、OSS 签名访问、AI 重试、人工复核和合作写操作，并通过逐路由跨成员测试证明前端隐藏无法绕过服务端授权
- [x] 14.3 增加按当前查看者首次发现日期的日期范围查询、稳定排序和游标分页，管理员支持按运营人员筛选；用重复采集、分配后可见、跨日期翻页和业务时区边界测试验证不重复、不遗漏、不错误跳组
- [x] 14.4 将页面“共享达人库”改为“达人库”，为运营提供今天、昨天、近 7 天、近 30 天和自定义日期筛选及日期分组，为管理员增加运营人员筛选，并用管理员、运营、只读成员浏览器回归验证可见范围和分页交互

验收记录：`npm run format:check`、`npm run lint`、`npm run typecheck`、`npm test`、`npm run build` 通过；页面回归 24 项通过、1 项因缺少真实数据库环境跳过（该项由下述流程覆盖）；`npm run test:mysql` 的领域/API 集成共 55 项及浏览器完整流程 1 项通过。桌面、候选详情、采集助手和 390px 窄屏截图已人工检查。外部抖音/OSS 使用受控替身，不替代第 11.8、12.1、12.5、12.6 项验收。
