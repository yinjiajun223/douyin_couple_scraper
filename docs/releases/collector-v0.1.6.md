# 采集助手 v0.1.6 发布说明

## 本次变化

- 单独出现“服务异常”“网络错误”“请求异常”、主文档 5xx 或白名单网络导航错误时，不再第一次命中就暂停；助手会保存检查点，并按“约 15 秒重载当前页 → 约 60 秒重建页面 → 约 5 分钟后使用同一持久画像重启可见浏览器”的有界阶梯恢复。
- 登录失效、验证码、安全验证、访问频繁、账号异常、HTTP 401/403/429 和无法可靠分类的异常仍立即安全暂停，不执行自动验证或循环刷新。
- 新增两分钟内复发升级、一小时三次上下文恢复熔断、可取消退避、浏览器代际管理以及 1 MiB × 3 文件的脱敏恢复日志。
- 本机控制页新增恢复类别、页面类型、阶段、次数、下次尝试时间和最后结果；恢复期间仍可人工暂停或终止。
- 筛选任务最长运行时间明确为 1–1,440 分钟，并提示多个停止条件任一先到即停止。24 小时是单次运行上限，不包含防休眠、进程守护、验证码处理或自动开始下一运行。
- 同时带入此前已完成但尚未发包的截图门控：只有硬筛 `pass` 的达人采集并上传截图，`fail` / `unknown` 仍同步结构化观察但不产生孤儿截图。

采集协议版本仍为 `1.0.0`，服务端最低采集器版本不提高，v0.1.5 仍可连接并用于回退。

## 平台发布状态

- Windows：本次目标发布平台。正式 ZIP 构建、包级冒烟、受控故障验收及小规模真实试跑已完成。用户已于 2026-09-23 确认取消两小时和 24 小时专项试跑，改为本人线上使用验收、发现问题后修复；两项不再作为发布前置门槛，也不记作测试通过。正式发布仍需完成下载交付和回退验证。
- 发布工作流本次选择 `include_macos=false`，仅发布 Windows 包；只有显式选择包含 macOS 且 Intel、Apple Silicon 两项冒烟均通过时，工作流才允许发布 macOS 包。
- macOS：源码与文档保持相同行为，但本次未在 Apple Silicon 和 Intel 两种真机上构建、冒烟和真实试运行，因此不发布或宣称 v0.1.6 macOS 包可用。后续必须使用同一个 universal 包分别完成两种架构验收。

## 回退

暂停当前运行并等待待同步数据清空，关闭 v0.1.6 浏览器与助手，从同级保留的 v0.1.5 目录启动。两个版本继续复用版本目录外的 `../data`；不要复制、删除或外传 DPAPI/Keychain 令牌与浏览器画像。

## 2026-09-23 首轮真实试运行记录

- 17:53（北京时间）启动本机 v0.1.6，确认设备已配对、原画像可用，待同步批次和证据均为 0；v0.1.5 目录保留。接电时系统睡眠超时为 0，电池模式为 900 秒，未修改系统设置。
- 运行 `16ecc990-48fd-44f3-ada1-a52641181469` 使用已有小规模规则：最多 100 条作品、60 分钟或 30 位候选，任一先到停止。首次作者核验后提示安全验证并暂停，作品、作者、候选计数均为 0，未执行自动重试。
- 排查发现作者页预解析安全检查错误地把完整 HTML 当作可见正文，正常页面中的隐藏验证脚本也会触发验证码提示。定向回归 `npm test -w @douyin/collector -- --run src/profile-inspection.test.ts -t '正常主页的隐藏验证脚本'` 在修复前失败，错误为 `DouyinProfileSafetyError / captcha_required`。
- 修复为读取可见正文，与运行时已有安全检查保持一致；正常隐藏脚本不再误报，实际可见验证文案、验证地址及 HTTP 限制规则仍保留。修复后作者核验回归 5 项全部通过。
- 初次暂停后保留 Chrome 页面等待人工核对；运营人员随后提供截图，显示正常作者主页、无可见验证码或安全验证弹窗，与隐藏脚本误报回归一致。此次短暂启动不算 2 小时或 24 小时验收通过。
- 修复后 `npm run check` 与 OpenSpec 严格校验通过。修正版测试包单独位于 `artifacts/canary-visible-text-fix/collector-windows-v0.1.6.zip`，SHA256 为 `b54dc9c47771d8401895c0f103b5547e92db8676a57259877fb8388507ed6b53`。旧的 `artifacts/collector-windows-v0.1.6.zip` 是修复前构建，不应用于正式分发。
- 修正版正式构建及包级冒烟均退出码 0，DPAPI 配对、画像和可见 Chrome 检查通过；该结果不能替代真实长运行验收。
- 18:05（北京时间）确认空闲且待同步为 0 后，通过本机控制接口关闭浏览器并切换修正版；旧 v0.1.6 安装目录重命名为 `collector-windows-v0.1.6-before-visible-text-fix`，v0.1.5 与共享 `data` 均保留。修正版继续安装在原 v0.1.6 同级目录，复用原配对和画像，随后人工授权继续同一运行。
- 18:05:53 检查：运行状态为 `running`，已核验并同步 1 位作者、1 条作品，候选为 0，待同步批次为 0，无恢复事件或安全暂停。长运行验收继续保持未完成，不自动开始下一次运行。
- 发布前按上述运行 ID 重新查询服务端，实际完成进度为 100 条作品、90 位作者、1 位硬筛通过、629 秒，待同步批次和证据为 0。用户确认时附图显示 96 位作者，但截图不含运行 ID；该数字不作为本次运行的精确服务端计数。
