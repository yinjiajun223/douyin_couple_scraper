# .agents
node_modules/
dist/
coverage/
artifacts/
playwright-report/
test-results/
*.tsbuildinfo
.env
.env.*
!.env.example
!.env.local.example

# 本地开发运行数据只保留在当前电脑。
.local/

# 采集结果和浏览器登录资料只保留在本机，严禁提交。
data/browser-profile/
data/screenshots/
data/*.csv
data/*.json
data/*.html

# 本地侧载镜像与发布前配置备份体积较大，只保留在交付电脑。
release/douyin-ops-images-*.tar
release/douyin-ops-images-*.tar.sha256
release/*.prev-*
