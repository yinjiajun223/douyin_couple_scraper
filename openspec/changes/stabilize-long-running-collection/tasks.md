## 1. 固化故障反馈回路与安全分类

- [x] 1.1 在 `apps/collector/src/fixtures/` 增加与现网截图一致、只含「服务异常，重新刷新获取数据」信号的脱敏页面夹具，并先在 `runtime-settings.test.ts` 写一个运行级回归：首次核验命中该页、后续页面恢复正常；修复前断言该用例确实因远端被改为 `paused` 而失败，记录定向测试命令和红灯输出。验证：该测试能捕获用户描述的精确暂停，而不是只调用分类函数。
- [x] 1.2 重构 `safety-gate.ts` 的 issue 类型和分类优先级，区分 `login_required`、`captcha_required`、`platform_restriction`、`transient_page_failure` 与 `low_parser_confidence`；登录/验证码/访问频繁/账号异常优先于暂时性文案，401/403/429 归人工事件，5xx、导航超时和连接中断归暂时性故障。验证：`npm test -w @douyin/collector -- --run src/safety-gate.test.ts` 覆盖文案重叠、状态码边界及未知信号 fail-closed。
- [x] 1.3 调整作者主页核验，在解析达人字段前捕获主文档状态和白名单化导航失败类型并执行安全检查，避免错误页先退化成身份不匹配或低可信度；不持久化正文、标题、完整 URL、请求头或响应体。验证：`profile-inspection.test.ts` 覆盖 503、导航超时、验证码与正常解析，且错误对象快照不含输入页面正文和主页地址。

## 2. 实现有界、可取消的恢复状态机

- [x] 2.1 给运行检查点增加向后兼容的可选 `recovery` 状态（类别、页面类型、阶段、次数、事件时间、下次尝试时间、最近结果和熔断计数），在 `normalizeCheckpoint` 中把 v0.1.5 旧检查点归一化为 idle。验证：新增测试从无 `recovery` 的旧 JSON 恢复，并确认原进度、待同步批次和低可信度数据不变。
- [x] 2.2 在运行时增加可注入时钟与可取消等待原语，退避期间轮询本地停止信号和远端运行状态；人工暂停、终止或浏览器被人工关闭时立即取消后续恢复。验证：使用假时钟推进 5 分钟退避，测试人工暂停无需等待完整退避且只保存一次最终检查点。
- [x] 2.3 实现单次暂时性事件的三级恢复控制器：约 15 秒后重载当前页、约 60 秒后重建作者页、约 5 分钟后用同一画像重启浏览器上下文；各阶段带随机抖动且只执行一次。验证：参数化运行级测试分别让三个阶段首次成功，均继续同一作者且不调用远端 pause。
- [x] 2.4 给浏览器上下文增加代际与计划关闭标记，区分内部恢复重启和运营人员关闭；任一时刻最多一个上下文、一个推荐页和一个作者页，替换上下文后清空旧 feed 缓存并复用同一持久画像。验证：测试断言计划重启不设置 `stopped`、人工关闭会暂停、旧代际迟到的 close 事件不影响新代际，打开页数始终有界。
- [x] 2.5 实现恢复后的短周期升级与滚动熔断：2 分钟内复发进入下一阶段，60 分钟内第 3 次完整上下文恢复触发暂停；恢复预算耗尽时显示已尝试阶段和人工建议。验证：假时钟测试覆盖窗口边界、成功后过窗重置及熔断恰好触发一次 pause。
- [x] 2.6 把恢复控制器接入作者页和推荐页安全检查，确保可信解析前不写入 `seenPosts` / `seenCreators` 或进度，已有 `pendingBatch` 仍优先同步，恢复耗时计入运行时长。验证：1.1 的回归转绿，并断言同一观察只发送一个批次、只上传一份合格截图、各进度计数只增加一次。

## 3. 增加脱敏诊断与控制页状态

- [x] 3.1 新增本机恢复事件日志模块，仅允许写入时间、运行 ID、进度计数、页面类型、分类代码、HTTP 状态或白名单导航错误、恢复阶段、浏览器代际和结果；单文件 1 MiB、最多 3 个轮转文件。验证：单元测试强制轮转并扫描输出不含 Cookie、Authorization、device token、页面正文、标题、请求头、响应体或完整个人主页地址。
- [x] 3.2 将当前恢复摘要加入 `CollectorRuntime.status()` 与运行装饰数据，并在本机控制页展示故障类别、恢复阶段、尝试次数、下次尝试时间和最后结果；退避中按钮仍可执行人工暂停/终止。验证：`control-server.test.ts` 与控制页测试覆盖恢复中、已恢复、熔断暂停三种状态和缺失旧字段的兼容显示。
- [x] 3.3 为普通错误补可理解但不泄露内部信息的消息：明确区分「正在自动恢复」「平台限制已暂停」「恢复预算耗尽」和「未知错误已安全暂停」。验证：collector 定向测试断言消息分类一致，且错误对象与控制接口响应均不包含敏感字段。

## 4. 明确 24 小时单次运行边界

- [x] 4.1 在共用停止条件表单给最长运行时间增加 `max="1440"` 和「最多 24 小时」说明，并提示多个停止条件任一先到即停止、需要按时间跑满时应清空会更早触发的非必需条件。验证：web 单元或 E2E 覆盖 1,440 分钟可提交、1,441 分钟被客户端/契约拒绝，以及只保留时长条件仍可创建任务。
- [x] 4.2 增加使用假时钟的 24 小时加速运行测试，期间注入多次可恢复的暂时性故障和重复推荐项，最终由 `maxDurationMinutes=1440` 正常完成。验证：无非预期 pause、无重复批次、无自动启动下一运行，浏览器页数、检查点大小和恢复日志数量保持在设计上限内。
- [x] 4.3 回归现有 `maxFeedItems`、`maxCreatorProfiles`、`targetCandidates` 与手动暂停/终止逻辑，确认任一条件仍可先于 24 小时结束运行。验证：contracts、domain run-service 与 collector 相关测试全部通过，规则 schema version 和批次协议版本不变。

## 5. 文档与发布包

- [x] 5.1 更新 `README.md`、`docs/product-manual.md`、`docs/operations/operator-guide.md`、`docs/collector-windows.md` 和 `docs/collector-macos.md`：说明暂时性故障的恢复阶梯、哪些情况仍立即暂停、24 小时的停止条件设置、电脑不得休眠及控制页诊断含义。验证：界面名称与代码一致，命令可完整复制，文档不承诺绕过风控或绝对 24 小时在线。
- [x] 5.2 将采集器版本升级到下一补丁版本并补发布说明；确认该包同时包含 `tighten-review-funnel` 已完成但尚未发包的「仅 pass 上传截图」改动。验证：版本常量、`package.json` 和构建产物一致，`git diff` 对 `COLLECTOR_PROTOCOL_VERSION` 与 `COLLECTOR_MIN_VERSION` 无提高。
- [x] 5.3 使用正式脚本构建 Windows 包并运行包级冒烟，确认从同级新目录复用 `../data` 的 v0.1.5 检查点、DPAPI 令牌和浏览器画像；保留旧目录与 SHA256 回退材料。验证：`scripts/build-collector-windows.ps1` 与 `scripts/test-collector-windows-package.ps1` 退出码为 0，包内不含 Cookie、明文令牌或开发依赖。
- [x] 5.4 macOS 源码和文档保持同一行为，但只有在 macOS 上构建通用包并分别通过 Apple Silicon / Intel 真机冒烟后才宣布该版本可用；否则明确记录本次仅发布 Windows 包。验证：对应架构验收记录或「未发布 macOS 包」说明存在，不用 Windows 结果替代。

## 6. 全量验证、线上验收与发布

- [x] 6.1 运行 collector 定向测试、`npm run check`、`npm run test:mysql`、`npm run test:e2e` 和 `openspec validate "stabilize-long-running-collection" --strict`。验证：所有命令退出码为 0，测试没有新增 skip/fixme，临时调试标记和故障夹具中的敏感内容扫描无命中。
- [x] 6.2 在 Windows 正式包上完成受控故障验收：暂时性「服务异常」可分别从页面重载和浏览器重启路径恢复，验证码/登录失效/访问频繁仍立即暂停，恢复期间人工暂停立即生效。验证：检查点、控制页和脱敏日志结论一致，服务端同一观察无重复记录。
- 6.3 已取消（2026-09-23，用户确认）：至少两小时真实 canary 不再执行，改由用户线上日常使用验收，发现问题后再修复。小规模试跑已获用户确认：100 条作品、96 位作者、1 位硬筛通过、0 次解析跳过；不以此替代两小时测试通过记录。
- 6.4 已取消（2026-09-23，用户确认）：24 小时专项真实运行不再执行，也不作为发布前置门槛；不记为测试通过。保留最长 1,440 分钟运行能力、现有自动化测试和人工安全暂停边界，不宣称已完成真实 24 小时稳定性验证。
- [ ] 6.5 发布新的 Windows 采集器给目标运营人员并随正常 web 发布上线 24 小时输入提示，保留 v0.1.5 回退路径且不强制旧客户端升级。验证：新包可 claim/同步/上传，旧 v0.1.5 仍不返回 426；回滚演练能暂停新包、关闭浏览器并从旧目录读取共享数据继续。
