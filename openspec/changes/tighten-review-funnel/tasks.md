## 1. 入库闸门（domain，阶段 A）

- [x] 1.1 修改 `packages/domain/src/ingestion/collector-ingestion.ts` 的 `findOrCreateCandidate`（:336-382）：未命中已存在行时仅在 `outcome === 'pass'` 才 INSERT，返回 `null` 表示未建候选；命中已存在行时保留 UPDATE 的 `latest_run_id`、`latest_creator_observation_id`、`version = version + 1`，并从 SET 子句中**移除 `hard_filter_status = ?`**（:358）。验证：`npm run typecheck -w @douyin/domain` 通过，且调用点已适配可空返回值。
- [x] 1.2 调整 `ingestCreatorObservation`（:182-334）：候选为 `null` 时跳过 `rule_evaluations` 写入（:306-331），不抛错、不影响同批次其余观测。验证：`npm run test -w @douyin/domain` 通过。
- [x] 1.3 在 `packages/domain/src/ingestion/collector-ingestion.integration.test.ts` 补闸门用例：pass 首次入库、fail 首次不入库、unknown 首次不入库、fail/unknown 观测仍写满 `creators` / `creator_observations` / `posts` / `post_observations` / `run_creator_sources`、非 pass 观测的 `rule_evaluations` 行数为 0、pass 观测的行数等于硬规则条数。验证：`npm run test:mysql` 通过（需本机 Docker 与 MySQL）。
- [x] 1.4 在同一集成测试文件补「一旦晋级持续跟踪」用例：已存在候选在后续运行结论为 fail / unknown 时，`latest_creator_observation_id` 与 `latest_run_id` 被更新、`hard_filter_status` 保持 `'pass'`、`version` 递增。验证：`npm run test:mysql` 通过。
- [x] 1.5 在同一集成测试文件补跨运行去重回归用例：同一 `platform_creator_id` 在两次运行中均为 fail，`creators` 只有一行且两条观测都挂在同一主档上。验证：`npm run test:mysql` 通过。
- [x] 1.6 在 `apps/api/src/ingestion.integration.test.ts` 锁定批次 ack 语义：fail 与 unknown 观测仍返回 `accepted`（重复提交返回 `duplicate`），任何情况下都不返回 `rejected`。验证：`npm run test:mysql` 通过，且 `git diff --stat packages/contracts` 为空。
- [x] 1.7 处理闸门对旧数据导入的连带影响（实施中发现，见 design.md D3）：`legacy-import.ts` 的 `progress_json.candidatesFound` 由 `importedObservations` 改为 0（旧导出无作品数据，硬筛恒为未知，不再产生候选），删除随之失效的 `preserveMissingLegacyPostEvidence`，并更新 `legacy-import.integration.test.ts` 断言为「候选为空、事实账本与来源关系照旧写入」。验证：`npm run test -w @douyin/domain` 与 domain 集成套件全绿。
- [x] 1.8 修正 `candidate-library.integration.test.ts`：`filter-old-fail` 不再由采集产生候选行，改为直接插入一行存量 `fail` 候选，以继续验证 `hardFilterStatus` 显式过滤对历史行生效。验证：domain 集成套件全绿。

## 2. 复核即推进阶段（domain + api，阶段 A）

- [x] 2.1 修改 `packages/domain/src/candidates/candidate-workflow.ts` 的 `submitManualReview`（:148-190）：在同一事务内依次执行 `assertCandidateVersion`（:490-494）→ INSERT `manual_reviews` → 仅当 `pipeline_status === 'pending_review'` 且 `decision !== 'pending'` 时更新为 `approved ? 'to_contact' : 'unsuitable'` 并 `appendCandidateEvent('pipeline_status_changed')` → `appendCandidateEvent('manual_reviewed')` → 写 `candidate.reviewed` 审计 → `version` 只递增一次。验证：`npm run typecheck -w @douyin/domain` 通过。
- [x] 2.2 在 `packages/domain/src/candidates/candidate-workflow.integration.test.ts` 补自动流转用例：`pending_review` + approved → `to_contact`、`pending_review` + rejected → `unsuitable`、`pending_review` + pending → 阶段不变、非 `pending_review` + approved → 记录结论但阶段不变且不报错、版本不匹配 → 冲突错误且 `manual_reviews` / `candidate_events` / `pipeline_status` / `version` 全部无变化。验证：`npm run test:mysql` 通过。
- [x] 2.3 在同一文件补单版本递增与事件断言：一次成功提交后 `version` 恰好 +1，`candidate_events` 恰好含 `pipeline_status_changed`（仅推进时）与 `manual_reviewed` 两条，审计表含一条 `candidate.reviewed`。**实施修正**：`candidate_events` 只有毫秒级 `created_at` 与随机 UUID 主键（`0004_candidate_decisions.sql:147-163`），同一事务内写入的两条事件在 `ORDER BY created_at DESC, id DESC` 下没有可依赖的先后次序，因此断言两条事件的存在与内容，不断言其相对顺序；不为排序新增序列列（超出本次范围）。验证：`npm run test:mysql` 通过。
- [x] 2.4 在同一文件补回滚用例：在写审计前注入失败，断言 `manual_reviews`、`candidate_events`、`pipeline_status`、`version` 均未被修改。验证：`npm run test:mysql` 通过。
- [x] 2.5 更新 `apps/api/src/server.ts` 的复核路由（:920）响应体，返回推进后的 `pipelineStatus` 与 version。**实施修正**：该路由本就透传 `submitManualReview` 的返回值，domain 增加 `pipelineStatus` 后无需改动 server.ts；改为在 `apps/api/src/ingestion.integration.test.ts` 的闸门用例末尾断言响应体与数据库一致。验证：`npm run test:mysql` 通过，`npm run typecheck -w @douyin/api` 通过。
- [x] 2.6 连带修正 `packages/domain/src/ingestion/collector-ingestion.integration.test.ts` 的复核链路：原先「复核 → 显式推进到 `to_contact`」在自动推进后会变成 `to_contact → to_contact`（不在流转矩阵内），改为断言复核自身已推进到 `to_contact`（version 2），再显式推进到 `contacted`（version 3）。验证：domain 集成套件全绿。

## 3. 未入库达人的判定依据（domain + api + web）

- [x] 3.1 在 `packages/domain/src/screening/` 新增重算能力：输入一次运行的 `runId`，从不可变的 `creator_observations` / `post_observations` 与 `campaign_rule_versions.rules_json` 复用 `evaluateHardFilters` 推导出结论及依据字段（粉丝数、命中作品点赞数与发布时间，缺失即标记未知，不得填 0）。**实施约束**：`postsWindowComplete` 未持久化（`packages/contracts/src/collector.ts:44` 为可选项，`creator_observations` 无对应列），重算固定传 `false`，与采集器硬编码值（`apps/collector/src/runtime.ts:484`）及旧导入的无作品数据情形一致，详见 design.md D2；不新增迁移或列。验证：新增集成测试覆盖「未入库达人可重算出 fail / unknown 及依据字段」「已入库达人重算结论与 `rule_evaluations` 一致」「缺失字段显示为 null 而非 0」，`npm run test:mysql` 通过。
- [x] 3.2 在 `apps/api/src/server.ts` 新增只读路由 `GET /runs/:runId/observed-creators`，可见范围为**工作区级 `campaign:read`**，与 `GET /runs`（:1011-1026）及三个 `/runs/:runId/{pause,resume,terminate}`（:1126-1166）一致。**实施修正**：原描述「复用 `assertRunOwnership`，运营人员只能读自己名下设备的运行」与现状不符 —— `assertRunOwnership`（`collector-ingestion.ts:143-164`）是设备侧校验（`JOIN run_devices ON device_id = ?` 且要求 `status = 'running'`），浏览器路由无法复用，且既有运行路由并无设备归属过滤；若新接口反而收窄，运营会在能打开的运行详情里看到 404 面板。跨工作区访问按 workspaceId 查不到，经 `handleCollectionRunError` 返回与「运行不存在」相同的 404 `COLLECTION_RUN_NOT_FOUND`，不泄露存在性。验证：在 `apps/api/src/runs.integration.test.ts` 补断言（同工作区运营可读且含未入库达人、跨工作区返回 404 且不存在的运行返回同一 404），`npm run test:mysql` 通过。
- [x] 3.3 在 `apps/web/src/pages/RunsPage.tsx` 的运行详情列新增面板，展示该运行观察到的达人、是否已入库、重算结论与依据字段；未识别字段显示「未知」而不是 0 或空。验证：`npm run typecheck -w @douyin/web` 与 `npm run build -w @douyin/web` 通过，并在浏览器中用一次真实运行的数据走查该面板（含至少一个未入库达人）。**实施备注**：走查落在 `tests/local-flow.e2e.spec.ts` —— 抖音夹具新增第二位达人「超范围同学」（粉丝 7000，超出默认 0–5000 上限），运行改为浏览 2 条作品，断言 `creator_observations` 2 行而 `campaign_candidates` 仍 1 行，运行详情面板显示「共 2 位 · 已入库 1 位」并对未入库达人给出「未入库 · 不进入复核队列」与「粉丝范围 · 不通过 · 范围 0–5000」。另在 `tests/web-access.e2e.spec.ts` 的运行监控用例补 mock 断言：缺失粉丝数显示「粉丝 未知」、缺失作品证据显示「1 条作品缺少赞数或发布时间，按未知处理」、结论徽标为「未知」。面板刷新令牌取 `${run.id}:${progress.creatorProfilesSeen}`，切换运行时先清空列表以免短暂串台，同一运行的进度刷新不清空以免每 3 秒闪空态。

## 4. 达人库分区轴切换（web + domain，阶段 B）

- [x] 4.1 确认 `packages/domain/src/candidates/candidate-library.ts` 的默认谓词 `candidates.hard_filter_status <> 'fail'`（:296-298）**保持不变**，并在该处加一行注释说明该列现表示「入库资格（创建时判定）」。验证：`npm run test:mysql` 中 `candidate-library.integration.test.ts` 全绿。**实施修正**：谓词没有保持不变，而是收紧为 `candidates.hard_filter_status = 'pass'`，注释一并改写。理由是原描述与两份既有事实冲突 —— (a) `specs/creator-candidate-library/spec.md:25` 明确要求「默认达人库 SHALL 仅包含已取得入库资格（即建立候选记录时硬筛通过）的达人。从未取得入库资格的达人——硬筛数据未知或明确不通过——MUST NOT 出现在默认达人库、任何跟进阶段分区、待复核统计或合作待办中」，`<> 'fail'` 会放行存量 `unknown` 行；(b) `packages/domain/src/dashboard/operations-dashboard.ts:38,43,49` 的三个计数器早已写死 `= 'pass'`，保留 `<> 'fail'` 会让 4.7 的「工作台三个计数器与达人库分区数字一致」在存在存量 `unknown` 行时必然失败（该行会计入分区却不计入计数器）。design.md D1「默认谓词无需改动」与 D4「存量 unknown 行随分区移除而自然不可见」的前提是前端做减法，但服务端默认口径必须独立成立，否则导出接口与其他消费方仍会取到存量 unknown 行。按 spec 修正实现，不修改 spec。验证：`npm run test:mysql` 全绿（domain 17 个测试文件、api 8 个测试文件）。
- [x] 4.2 在 `candidate-library.integration.test.ts` 补存量数据隔离用例：历史 `fail` 行在默认列表不可见、历史 `unknown` 行在默认列表可见但不再被任何前端分区消费、`hardFilterStatus` 显式过滤仍能取回两者且受同一记录级可见范围约束。验证：`npm run test:mysql` 通过。**实施修正**：随 4.1 的谓词收紧，历史 `unknown` 行在默认列表中同样不可见（spec:25 的 MUST NOT 覆盖「未知或明确不通过」两种情形），用例名改为「存量 fail 与 unknown 行都不进默认列表，显式过滤仍受同一记录级可见范围约束」。用例内容：一次运行采集三位达人（`legacy-fail` 粉丝 7000 越界、`legacy-unknown` 粉丝数缺失、`fresh-pass` 粉丝 1200 且命中 12000 赞作品），闸门后只有 `fresh-pass` 产生候选行，另两行存量候选按 1.8 的方式手工插入（`fail` / `unknown`）；断言默认列表只含 `fresh-pass`，三种显式 `hardFilterStatus` 各取回对应一行；再以 `createInvitation` + `acceptInvitation` 建一位运营，断言其显式 `hardFilterStatus=fail` 检索为空（设备属于管理员，既未采集也未被分配，记录级可见范围不被显式过滤绕过），把该 `fail` 行 `assignee_user_id` 改派给该运营后可取回一行，但默认列表仍为空（默认谓词与记录级可见范围取交集）。验证：`npm run test:mysql` 通过，该文件 2/2 全绿。
- [x] 4.3 将 `apps/web/src/pages/CandidatesPage.tsx` 的 `candidateSection`（:39）由 `'qualified' | 'needs_evidence'` 改为五个 pipeline 分区（待复核 / 待联系 / 跟进中 = contacted + communicating / 已合作 / 不合适 = unsuitable + declined），移除「证据不足」分区及其文案。验证：`npm run typecheck -w @douyin/web` 通过，`grep -rn "needs_evidence\|证据不足" apps/web/src` 无结果。**实施备注**：分区常量落在 `apps/web/src/constants.ts` 的 `candidateSections`，状态集合由 `satisfies ReadonlyArray<{ statuses: readonly PipelineStatus[] }>` 约束，写错阶段名会在 typecheck 阶段失败。除五个阶段分区外**另加一个跨阶段的「全部」分区并作为默认**（spec:23 写的是「至少提供五个分区」，六分区不违反；分区轴仍是 pipeline，不违反「MUST NOT 再以硬筛结论作为主分区轴」）。**为什么必须有「全部」**：工作台第三个计数器 `my_assignments`（`operations-dashboard.ts:47-50`）只按负责人过滤、**不按阶段过滤**，`onOpenCandidates('mine')` 的语义是「我负责的全部达人」；若只有五个单阶段分区，该卡片点进去必然与卡片数字对不上，4.7 要求的「工作台三个计数器与达人库分区数字一致」就无法成立。列表项右侧的徽标由硬筛结论换成合作阶段（`pipelineStatusLabel` + 新增 `.candidate-stage` 样式）：闸门后默认列表里每行硬筛恒为「通过」，原徽标已成纯噪声，且 spec:25 要求默认界面不得暴露硬筛结论轴；而「全部 / 跟进中 / 不合适」三个分区各自跨多个阶段，阶段徽标才是有信息量的那一列。验证：`npm run typecheck -w @douyin/web`、`npm run build -w @douyin/web` 通过，`grep -rn "needs_evidence\|证据不足" apps/web/src` 无结果。
- [x] 4.4 从 `CandidatesPage.tsx` 的查询构造（:60）移除 `hardFilterStatus`，并修改 `apps/web/src/lib/date.ts` 的 `candidateMatchesLibraryView`（:27-36）去掉硬筛判断、改为按分区对应的 pipeline 集合匹配。验证：`npm run typecheck -w @douyin/web` 通过，`grep -rn "hardFilterStatus" apps/web/src` 无结果。**实施修正**：分区过滤必须落在服务端，不能只做前端匹配 —— spec:21 要求「服务端分页」、spec:49 要求「分页过程中不重复或遗漏」、spec:51-53 要求「系统只返回授权范围内处于该阶段的达人」；若前端在整页 50 条里筛阶段，「已合作」这类稀疏分区会显示近乎为空却仍挂着「加载更多」，运营要连点十几次才凑出一页，且计数与分页都对不上。而既有 `candidateFilterSchema` 只支持单值 `pipelineStatus`，「跟进中」（contacted + communicating）与「不合适」（unsuitable + declined）各需两个值。因此新增 `pipelineStatuses` 数组过滤：domain 侧 `candidate-library.ts` 抽出 `pipelineStatusSchema` 供单值与多值共用，多值走 `candidates.pipeline_status IN (...)` 并对入参去重；api 侧 `GET /candidates` 新增 `pipelineStatuses` 查询参数，按既有 `tags` 的逗号分隔约定解析；web 侧 `buildCandidateQuery` 传 `candidateSection.statuses.join(',')`，不再传 `hardFilterStatus` 与 `pipelineStatus`。`candidateMatchesLibraryView` 按原描述保留并改为匹配分区的阶段集合，但它的职责收窄为**兜底**：`loadMoreCandidates` 没有中断机制，切换分区后仍可能有一次旧分区的响应回来，直接合并会把两个分区的达人混在一起。同一理由下给列表请求加了 `libraryRequest` ref 令牌（沿用既有 `detailRequest` 的写法），筛选条件一变即作废在途的「加载更多」，否则旧分区的 `nextCursor` 会覆盖新分区的，翻页随之重复或漏掉达人 —— 那正是 spec:49 禁止的情形。`hardFilterStatus` 同时从 `apps/web/src/types.ts` 的 `CandidateSummary` 与 `CandidateDetailData.candidate` 中删除，使 spec:25「默认界面 MUST NOT 提供该入口」由类型系统兜住（服务端仍返回该字段，前端不再声明也不再消费）；`hardFilterLabel` 保留，规则证据卡片仍用它渲染 `evaluation.outcome`。按 design.md D8「分区轴与既有 preset 过滤本就是同一维度」，`preset` 不再是与分区正交的第二个过滤器：`'pending_review'` / `'to_contact'` 直接决定初始分区，`'mine'` 落到「全部」并叠加 `ownerUserId`，`OperationsDesk.tsx` 与 `TodayPage.tsx` 无需改动。验证：`npm run typecheck -w @douyin/web` 通过，`grep -rn "hardFilterStatus" apps/web/src` 无结果，`packages/domain/src/candidates/candidate-library.integration.test.ts` 新增用例「阶段分区用多值过滤在服务端生效，且游标翻页不重复不遗漏」覆盖五个分区的阶段集合互不重叠、多值与默认「入库资格」谓词叠加（存量 fail 行不进任何分区）、单值 `pipelineStatus` 保持可用，并以 `limit: 2` 走完游标翻页断言无重复无遗漏（八条观测 `observed_at` 相同使 `first_visible_at` 完全并列，正好覆盖游标的并列兜底分支）。
- [x] 4.5 保持 `groupCandidatesByDate`（`apps/web/src/lib/date.ts:39-55`）与日期筛选选项完全不动。验证：`grep -n "Asia/Shanghai" apps/web/src/lib/date.ts` 仍命中，`npm run build -w @douyin/web` 通过，浏览器中确认「今天 / 昨天 / 近 7 天 / 近 30 天 / 自定义」分组与分页行为与改动前一致。**实施备注**：`git diff -U2 apps/web/src/lib/date.ts` 只有两处变化 —— 顶部新增 `candidateSections` / `CandidateSection` 两个导入，以及 `candidateMatchesLibraryView` 函数体；`candidateDateRange`、`groupCandidatesByDate`、`businessDateKey`、`shiftDateKey` 逐字节未动，`candidateDateOptions` 亦未动。`grep -n "Asia/Shanghai" apps/web/src/lib/date.ts` 命中第 64 行。分组与分页行为由 `tests/web-access.e2e.spec.ts` 的「达人库支持日期分组、管理员成员筛选和游标加载更多」用例覆盖（该用例只把 mock 的入参判据从 `hardFilterStatus` 换成 `pipelineStatuses`，断言未改），日期分组逻辑本身没有改动因此无需新增走查。
- [x] 4.6 让 `CandidatesPage.tsx` 的 `submitWorkflowRequest`（:367-397）在成功后刷新数据：重新拉取该达人详情（`openCandidate`）并重新加载当前分区列表（给 :73-101 的 `useEffect` 加一个刷新令牌依赖）。**为什么必须做**：复核现在会在同一次请求内推进阶段并递增版本，而当前实现提交后不重新拉取，详情面板会同时显示过期的阶段与过期的 `candidateVersion`，运营紧接着的第二次操作必然 409。**提前实施说明**：本项在组 3 期间提前完成，因为它同时是 3.3 浏览器走查的前置条件。**顺带修复既有回归**：`bc4024a refactor(web): split App.tsx into modules and drop AI UI` 声称「纯物理搬迁、行为不变」，却丢掉了原 `App.tsx` 中 `submitWorkflowRequest` 成功路径末尾的 `onQueued()`（等价于 `openCandidate(detail.candidate.id)`），导致拆分后任何一次保存都不再刷新详情面板；`tests/local-flow.e2e.spec.ts` 的「最新人工结论：通过」断言在本次改动前就已经失败。实施方式：`CandidateDetailView` 新增 `onSaved` 回调并在成功分支调用，父组件的 `refreshAfterSave` 置一个 `keepSelection` ref 后递增 `libraryToken`，使列表重拉时不清空当前选中的达人。验证：`npm run typecheck -w @douyin/web` 通过，`npm run test:e2e` 21 项全绿，其中 `local-flow` 已断言复核保存后阶段立即变为「待联系」且 `input[name="nextStatus"]` 为 `to_contact`，无需手动刷新即可继续保存联系资料。
- [x] 4.7 浏览器走查五个分区：新建一次采集运行后确认只有 pass 达人出现在「待复核」，点击复核通过该达人自动出现在「待联系」，点击不通过则出现在「不合适」，且工作台三个计数器与达人库分区数字一致。验证：走查记录写入本 change 的实施备注，截图不入仓。**实施备注**：沿用 3.3 的做法，走查落在 `tests/local-flow.e2e.spec.ts`（真实 API + MySQL + 浏览器），不另建走查文档。抖音夹具由两位达人扩到三位 —— 新增「二号同学」（`second-classmate`，粉丝 1500，同样命中 12000 赞作品）用于走「复核不通过」这条腿，「流程测试同学」走「复核通过」，「超范围同学」（粉丝 7000 越界）仍用于验证未入库达人不产生候选行；`最多浏览作品` 由 2 改 3，断言随之改为 `creator_observations` 3 行 / `campaign_candidates` 2 行 / `media_objects(confirmed)` 2 行、`uploads.size` 2、运行详情「共 3 位 · 已入库 2 位」。走查步骤与断言：进入达人库后默认分区「全部」按钮 `aria-pressed=true`、`.library-count` 为「2 位达人」且「超范围同学」在列表中不存在（`toHaveCount(0)`）；点「待复核」得「2 位待复核」；选中「流程测试同学」填理由并保存人工结论（结论默认「人工通过」），断言「最新人工结论：通过」、跟进区块显示「待联系」、`input[name="nextStatus"]` 为 `to_contact`，且 `.library-count` 立即变为「1 位待复核」、该达人的列表按钮 `toHaveCount(0)`（复核在服务端同一次请求内推进阶段，列表自动离开原分区）；随后保存联系资料并指定负责人；再选中「二号同学」，把「结论」下拉切到「不符合」并填理由保存，断言「最新人工结论：不符合」、跟进区块显示「不符合」、`.library-count` 变为「0 位待复核」；点「不合适」得「1 位不合适」且「二号同学」在列；最后进入「今日工作」，断言三张 `.dashboard-card` 的数值为 待复核 0 / 待联系 1 / 我负责的 1，与达人库分区数字同口径（`my_assignments` 不按阶段过滤，故落在跨阶段的「全部」分区上，这正是 4.3 加第六个分区的原因）。**走查暴露并修复的缺陷**：`CandidatesPage.tsx` 的两栏容器原来以 `visibleCandidates.length ?` 为条件，复核把达人推出当前分区后若该分区被清空，整个 `.candidate-workspace` 会连同详情面板一起卸载，运营刚点完「保存人工结论」就看不到「最新人工结论：不符合」这行确认文案，只能靠猜。这在旧的单一默认列表下很少触发，但换成六个分区后「保存即清空当前分区」成为常态，且与 4.6「提交后自行刷新、运营无需手工刷新即可继续操作」的目标直接冲突。修法是把条件改为 `visibleCandidates.length || selectedCandidateId`，并把分区空态文案抽成 `emptyLibraryText` 在列表栏内复用；切换分区时 `showCandidateSection` 仍会清空选中项，因此空分区不会残留上一个分区的详情。截图新增 `library-unsuitable-section.png`，与既有截图一样写入 Playwright `test-results/`，不入仓。验证：`npm run lint`、`npm run typecheck`、`npm run build` 全部通过；`npm run test:mysql` 全绿（domain 17 个文件 51 项、api 8 个文件 16 项、`local-flow` 1 项通过 30.9s）；`npm run test:e2e` 20 项通过、1 项跳过（`local-flow` 需要 `MYSQL_TEST_URL`，已在上一步覆盖）。

## 5. 孤儿截图回收（worker + domain，阶段 C，开关默认关闭）

- [x] 5.1 新增两个配置项：孤儿回收启用开关（默认关闭）与宽限期天数（默认 7），与既有 `MEDIA_RETENTION_DAYS`、`MEDIA_CLEANUP_BATCH_SIZE` 同一加载与校验风格。验证：`packages/domain/src/config.test.ts` 补默认值与非法值用例，`npm run test -w @douyin/domain` 通过。**实施备注**：`workerConfigSchema` 末尾新增 `ORPHAN_MEDIA_CLEANUP_ENABLED`（默认 `false`）与 `ORPHAN_MEDIA_GRACE_DAYS`（`z.coerce.number().int().min(1).max(3650).default(7)`）。**开关不用 `z.coerce.boolean()` 而新增 `booleanFlag` 辅助器**：环境变量没有布尔类型，任何宽松解析都会把 `yes` / `on` / 拼错的值静默当成关闭（或更糟，当成开启），而这个开关控制的是**不可恢复的 OSS 对象删除**，必须只接受明确字面量 `true/false/1/0`，其余一律在启动阶段报 `ConfigValidationError` 并只回显字段名。宽限期取 7 天的理由：它必须长于「素材确认 → 候选行建立」的最大正常间隔，否则采集与入库之间的正常延迟会被误判为孤儿；首次上线取保守值，等观测到实际间隔后再收紧。验证：`config.test.ts` 的既有用例「Worker 应用可配置的素材保留与清理默认值」补两个默认值断言与四键覆盖断言，另加用例「孤儿回收开关只接受明确字面量，宽限期必须是 1 到 3650 天的整数」遍历 `['yes','on','TRUE','']` 均抛 `ORPHAN_MEDIA_CLEANUP_ENABLED`、`'0'` 解析为 `false`、`['0','-1','7.5','abc','3651']` 均抛 `ORPHAN_MEDIA_GRACE_DAYS`；`npm run test -w @douyin/domain` 9 个文件 34 项全绿。
- [x] 5.2 在 `infra/production` 的 compose / env 模板中补这两个变量的占位符，并在 `scripts/preflight-production.mjs` 中登记，确保缺省时不报错、显式关闭时不执行回收。验证：`node scripts/preflight-production.mjs` 不因新变量失败，且模板中不含任何真实秘密值。**实施备注**：`.env.production.example` 在 `MEDIA_CLEANUP_INTERVAL_MS` 之后补两行默认值（`false` / `7`）及「默认关闭、确认后显式开启」的说明；`infra/production/compose.yml` 的 worker 环境用 `${ORPHAN_MEDIA_CLEANUP_ENABLED:-false}` / `${ORPHAN_MEDIA_GRACE_DAYS:-7}`，`compose.smoke.yml` 按其文件既有风格写死 `"false"` / `7`。`scripts/preflight-production.mjs` 的 `validateProductionEnvironment` 在 `return errors` 之前登记这两个键，**允许缺省**（应用侧默认关闭，缺省是安全状态）但**一旦写了就必须能被明确解析** —— `yes` 之类会在应用侧被 `booleanFlag` 拒绝，宁可预检阶段就拦下来，也不要等 worker 启动失败。验证：以 `parseEnvFile` 读 `.env.production.example` 后调用校验器，as-is 与「两键全删」均无 ORPHAN 相关错误，`true`+`30` 无错误，`yes` / `7.5` / `0` 各返回对应的那一条中文错误；模板中只有占位值，无任何真实秘密。
- [x] 5.3 在 `packages/domain/src/media/media-cleanup.ts` 的 `cleanupMediaObjects`（:28-92）新增孤儿回收路径：目标为 `status = 'confirmed'` 且其 `creator_observation_id` 找不到对应 `campaign_candidates` 行、且确认时间早于宽限期的对象；复用既有两阶段删除（claim → `expired` → `deleteObject` → `deleted`，:104-123），`media_objects` 行永不物理删除。验证：新增 `media-cleanup.integration.test.ts`，覆盖超期孤儿被删、未超期保留、有候选行保留、中断后重跑不重复删除、行未被物理删除五个用例，`npm run test:mysql` 通过。**实施备注**：新增私有函数 `selectOrphanRows`，判据用 `NOT EXISTS (SELECT 1 FROM campaign_candidates WHERE workspace_id = media.workspace_id AND creator_id = creator.creator_id)` —— 注意是按 **creator** 而非按 observation 关联，因为入库资格是达人级的（`uq_campaign_candidates_campaign_creator`），同一达人的新观测不该让旧观测的截图逃过回收。**`creator_observation_id IS NULL` 的素材一律不回收**：内连接天然排除它们，这是刻意的 —— 无法判定归属而删除不可逆，宁可留下也不误删（这类行仍由既有留存路径按保留期处理）。`MediaCleanupSummary` 新增 `deletedOrphans` 字段，`DEFAULT_ORPHAN_MEDIA_GRACE_DAYS = 7` 从 `packages/domain/src/index.ts` 导出，`assertCleanupOptions` 增加 `orphanGraceDays` 的 1–3650 整数校验。**孤儿路径排在留存路径之后**：被留存路径 claim 过的行状态已不是 `confirmed`，孤儿查询天然取不到，两条路径不可能重复 claim 同一对象（5.4 有对应用例）。claim→deleteObject→deleted 两阶段删除完全复用既有 `deleteRows`，未新增第二条删除逻辑，`media_objects` 行始终保留为 `deleted`。测试文件 `media-cleanup.integration.test.ts` 共 7 个用例，本项要求的五个都在其中：「超过宽限期的孤儿截图被删除，素材行保留为 deleted 而不被物理删除」（同时覆盖超期被删与行未物理删除，并断言 `COUNT(*) = 1`）、「宽限期内的孤儿截图保留，等待入库流程完成」、「已有入库候选行的素材即使远超过宽限期也不回收」（400 天仍保留，并断言候选行确实存在，避免用例因播种失败而假绿）、「回收中断后重跑不重复删除也不漏删」。中断用例的关键发现：**重跑的补删不是由孤儿路径完成的**，而是由既有的 unconfirmed 路径完成 —— 首轮 `deleteObject` 失败后行停在 `expired`，而 `selectOrphanRows` 只取 `confirmed`，因此第二轮把它当作「已过期未确认」重试，`deletedOrphans` 保持 0、`deletedUnconfirmed` 变 1、`deleteCount(key)` 恰为 1。这说明两阶段删除的中断恢复不需要为孤儿路径另写一套。用例之间共享一个工作区且回收是工作区级的，文件头注释记录了顺序无关策略：每条用例只播种自己的行、「被保留」的行用小于宽限期的确认时间、「已删除」的行状态不再是 confirmed，因此不会被后续用例二次消费。验证：`npm run test:mysql` 全绿。
- [x] 5.4 在同一测试文件断言孤儿回收路径与既有「已归档任务 + 超保留期」路径（:58-80）互不干扰：同一对象不会被两条路径重复 claim，开关关闭时两条既有行为完全不变。验证：`npm run test:mysql` 通过。**实施备注**：两个用例。「同一对象同时符合孤儿与归档留存判据时只被 claim 一次」：`beforeAll` 里建了一个**已归档**的任务 B 及其中一位硬筛不通过的达人（无候选行），给它插一条 400 天前的 confirmed 素材 —— 它同时满足孤儿判据与「已归档 + 超保留期」判据；该次调用把 `orphanGraceDays` 调到 365，使自己成为唯一符合孤儿判据的行，`retentionDays: 1` 使留存路径必然选中它。断言 `deletedConfirmed: 1`、`deletedOrphans: 0`、`deleteCount(key) === 1`、行状态 `deleted` —— 即留存路径先 claim，孤儿路径随后取不到，OSS 对象只被删一次。「开关关闭时孤儿不回收，既有两条回收路径行为不变」：`orphanCleanupEnabled` 缺省（即 `false`）时，一条 8 天前的孤儿素材保持 `confirmed` 且 `deleteCount` 为 0，而同一次调用里既有两条路径照常工作（`deletedUnconfirmed: 1` 来自一条过期的 pending 行、`deletedConfirmed: 1` 来自归档任务 B 里 300 天前的行，`retentionDays: 1`），证明新开关只关掉新路径、不改变既有行为。验证：`npm run test:mysql` 全绿（domain 18 个文件 60 项、api 8 个文件 16 项、`local-flow` 1 项）。
- [x] 5.5 用 `EXPLAIN` 确认回收查询走 `media_objects.creator_observation_id` 与 `campaign_candidates.creator_id` 上的索引且受批大小上限约束；若缺索引，追加一个向前的 expand 迁移（不修改任何已执行过的迁移文件）。验证：`EXPLAIN` 输出记录在实施备注中，`packages/domain/src/database/query-plans.integration.test.ts` 全绿，`npm run test:mysql` 通过。**实施修正：任务原文对索引的预期有一半不成立，而真正的缺口在别处，已按原文预留的「若缺索引则追加 expand 迁移」分支处理。**（a）`campaign_candidates.creator_id` 那一半成立：反连接确实走 `idx_campaign_candidates_creator`。（b）`media_objects.creator_observation_id` 那一半不成立：在真实数据量下 `media_objects` 是驱动表，`creator_observations` 经 PRIMARY 做 `eq_ref`，`idx_media_objects_creator_observation` 只有在 `creator_observations` 极小时才会被选中（首次实测时该表只有 7 行，优化器就以它为驱动表，`media` 侧退化成 `ref`）。这不是缺陷，只是原文对连接方向的假设与实际相反。（c）**真正的缺口是没有任何索引以 `status` 打头**：既有 `idx_media_objects_workspace_status` 以 `workspace_id` 打头，而 worker 传入的 `workspaceId` 是 `NULL`，`(? IS NULL OR workspace_id = ?)` 因此不可用作索引条件。后果是孤儿路径**与既有的留存路径**都对 `media_objects` 全表扫再 filesort，代价随证据总量线性增长且每小时重跑一次 —— 恰好是本次改动想控制的成本项。
  **实测数据**（一次性脚本，跑完即删；5000 creators + 5000 observations + 5000 media + 250 candidates，其中 95% 的达人无候选行，即入库闸门后的真实分布）：加索引前孤儿查询 `media` 为 `type=ALL, key=NULL, rows=5247, Extra="Using where; Using filesort"`，留存路径的 `possible_keys` 甚至为 `NULL`；加 `(status, confirmed_at)` 后两条路径都变成 `type=range, key=idx_media_objects_status_confirmed, key_len=89, Extra="Using index condition; Using where"`（无 filesort、无 temporary），`ORDER BY confirmed_at` 由索引天然满足，`LIMIT 100` 可提前终止扫描。同一数据集上孤儿查询实测耗时 5730µs → 1173µs。
  **因此新增 `packages/domain/migrations/0014_media_cleanup_index.sql`**，内容只有一条 `ALTER TABLE media_objects ADD KEY idx_media_objects_status_confirmed (status, confirmed_at)`（InnoDB 加二级索引为 INPLACE 在线 DDL，不阻塞 worker）。未修改任何已执行过的迁移文件；在全新库上 `npm run migrate -w @douyin/domain` 输出 `applied: 0001…0014, skipped: []`。
  **批大小上限**无法由 `EXPLAIN` 表达，改为在 `media-cleanup.integration.test.ts` 做行为断言：播种确认时间分别为 102 / 101 / 100 天前的三条孤儿素材，以 `batchSize: 2` 调用，断言 `deletedConfirmed: 0`、`deletedOrphans: 2`、最老两条的 key 各被删一次、第三条 `deleteCount` 为 0 且行仍为 `confirmed`。三行都比其他用例遗留的 confirmed 孤儿行（1 天与 8 天）更老，`ORDER BY confirmed_at ASC` 保证批内先取到它们，因此与用例执行顺序无关。
  **`query-plans.integration.test.ts` 新增两条计划断言**（孤儿路径 + 留存路径，两条都用同一个新索引），并补种 1000 位无候选行的达人与 1000 条 `media_objects`，`confirmed_at` 按稳态分桶：85% 在近 7 天内（两条路径都不命中）、10% 超过 7 天宽限期（只命中孤儿路径）、5% 超过 180 天保留期（两条都命中）。**为什么必须分桶**：若把 1000 行的确认时间均匀铺在 900 天里，孤儿路径命中率约 99%，优化器会判定全表扫比索引区间扫更便宜而放弃该索引 —— 首次实现正是这样失败的（`expected null to be 'idx_media_objects_status_confirmed'`）。分桶后的分布也正是生产稳态：worker 每小时一轮、每轮批量 100，积压排空后每轮只有少量新过期的行。
  **该 fixture 上的实测 `EXPLAIN`**（1000 条 media，其中 900 条 confirmed、150 条超过 7 天宽限期）：`media` → `type=range, key=idx_media_objects_status_confirmed, key_len=89, rows=150, Extra="Using index condition; Using where"`；`creator` → `type=eq_ref, key=PRIMARY, rows=1`；`candidates` → `type=ref, key=idx_campaign_candidates_creator, key_len=72, rows=1, Extra="Using where; Not exists; Using index"`。`rows=150` 恰为符合判据的行数，说明区间扫没有多读。留存路径同形：`media` 走同一索引的 range，四个 LEFT JOIN 全部 PRIMARY `eq_ref`，无 filesort。
  **一个需要记录的副作用**：在 5000 行数据集上加索引后，优化器把 `NOT EXISTS` 从相关子查询改为半连接物化（`<subquery2>` 对 `<auto_distinct_key>` 做 `eq_ref`，另有一行 `select_type=MATERIALIZED` 扫 `idx_campaign_candidates_creator`），即每轮会构建一次覆盖 `campaign_candidates` 的临时表；在 1000 行 fixture 上则仍是相关 `ref` 形式。两种形式都只走索引，都远优于原来的全表扫 + filesort，且临时表大小随候选行数（而非证据总量）增长，因此**不加优化器提示去固定策略** —— 提示会带来跨 MySQL 版本的脆弱性。验证：`query-plans.integration.test.ts` 4/4 全绿，`npm run test:mysql` 全链路通过（`scripts/run-mysql-integration.mjs` 对每个阶段 fail-fast，退出码 0 即代表 domain、api、`local-flow` 三段全绿）。
- [x] 5.6 在 `apps/worker/src/index.ts` 接入该回收任务，沿用既有轮询与错误记录方式，失败不阻塞其他后台任务。验证：`npm run typecheck -w @douyin/worker` 与 `npm run build -w @douyin/worker` 通过，本地以开关开启运行一轮并确认日志记录回收数量且不含对象存储永久地址。**实施备注**：没有新增轮询循环 —— `runMediaCleanupOnce` 的 `Pick<WorkerConfig, …>` 补上两个新键并透传给 `cleanupMediaObjects`，孤儿回收因此自动搭既有的 `MEDIA_CLEANUP_INTERVAL_MS` 定时器与 `cleanupRunning` 重入保护；异常仍由既有的 `try/catch` 收敛为 `media.cleanup.failed`，只记 `errorType`，不阻塞后续轮次。成功日志沿用既有的 `{ event: 'media.cleanup.completed', ...summary }`，因 summary 多了 `deletedOrphans` 字段而自动带出计数，**日志里没有任何对象存储地址、bucket 名或 object key**（summary 是五个纯数字）。本地实跑验证：容器全新迁移后手工插入一条 30 天前确认、所属达人无候选行的孤儿素材，OSS 端点指向不可解析的 `https://oss-worker-check.invalid`（不产生任何外网流量），`node apps/worker/dist/index.js` 各跑一轮 —— 开关 `false` 时五个计数全 0、孤儿素材原样保留；开关 `true` 时日志为 `{"event":"media.cleanup.completed","deletedConfirmed":0,"deletedOrphans":0,"deletedUnconfirmed":0,"failed":1,"preservedReferenced":0}`，进程未崩溃、未打印堆栈或 object key，该行被 claim 为 `expired`；再跑第三轮仍是 `failed: 1`，且补删由既有 unconfirmed 路径承担（孤儿路径只取 `confirmed`，因此不会重复 claim），行始终存在未被物理删除。三轮结果与 `media-cleanup.integration.test.ts` 的中断重跑用例完全一致。验证：`npm run typecheck`、`npm run build` 全仓通过。

## 6. 采集器一行改动（阶段 D，搭车发布）

- [x] 6.1 将 `apps/collector/src/runtime.ts:506-511` 的截图条件由 `filter.outcome === 'fail'` 改为 `filter.outcome !== 'pass'`，并确认 `flushPending`（:657-692）的上传调用与 `media-upload.ts` 的抛错行为均无需改动。验证：`npm run typecheck -w @douyin/collector` 通过，`git diff --stat packages/contracts apps/collector/package.json` 为空，`grep -n "COLLECTOR_MIN_VERSION\|COLLECTOR_PROTOCOL_VERSION" -r apps packages` 无本次改动。**实施备注**：改动落在 `runtime.ts:509`，`git diff -- apps/collector/src/runtime.ts` 只有这一个 hunk（一行条件 + 两行说明为什么的注释）。**`flushPending` 无需改动**已逐行确认：它的上传分支是 `if (state.screenshot)`（:681），条件为 null 时整段跳过，末尾 `state.pendingBatch = null; state.screenshot = null;`（:690-691）本来就与结论无关；`state.screenshot` 也不会残留到下一条达人，因为每条都在 :508 重新赋值。**`media-upload.ts` 无需改动**同样确认：它只在已有截图时被调用，四个错误码（`unpaired_device` / `api_request_failed` / `invalid_signed_upload` / `object_upload_failed`）都与硬筛结论无关。验证：`npm run typecheck -w @douyin/collector` 退出 0；`git diff --stat packages/contracts apps/collector/package.json` 输出为空；`grep -rn "COLLECTOR_MIN_VERSION\|COLLECTOR_PROTOCOL_VERSION" apps packages --include=*.ts`（排除 `dist/` 与测试）的 12 处命中全部是既有引用，`COLLECTOR_PROTOCOL_VERSION` 仍为 `1.0.0`，`COLLECTOR_MIN_VERSION` 默认仍为 `0.1.0`（`packages/domain/src/config.ts:39-42`），本组未改任何版本常量。
- [x] 6.2 在 `apps/collector/src/profile-inspection.test.ts` 或就近测试中补断言：pass 生成截图、fail 与 unknown 均不生成截图、三种结论的结构化观测都照常入队。验证：`npm run test -w @douyin/collector` 通过。**实施备注**：**位置改为 `apps/collector/src/runtime-settings.test.ts`** —— `profile-inspection.test.ts` 只测 `inspectDouyinCreatorProfile`，它既不跑硬筛也不做截图决策，在那里加断言等于断言不到被测行为；`runtime-settings.test.ts` 是仓库里唯一构造真实 `CollectorRuntime` 并驱动发现循环的文件，新增用例复用了它既有的画像页/推荐页 mock 骨架。
  **一个必须记录的可达性发现**：`unknown` 不可能由「缺粉丝数」产生。缺粉丝数时 `calculateProfileConfidence`（`packages/platform-douyin/src/authors.ts:307-321`）只得 `0.3(profileUrl) + 0.3(nickname) + 0(followerCount) + 0.1(biography) = 0.7`，低于 `DOUYIN_MIN_PARSER_CONFIDENCE = 0.75`（`apps/collector/src/safety-gate.ts:3`），于是 `runtime.ts:426` 的 `low_parser_confidence` 分支直接跳过该达人、**根本不构造观测**。因此采集端唯一可达的 `unknown` 来自 `recent-post-likes`：`runtime.ts:484` 把 `postsWindowComplete` 硬编码为 `false`，`hard-filter.ts:98-102` 于是永远走 `uncertainPosts.length > 0 || postsWindowComplete === false` 这一支，缺少达标作品时判 `unknown` 而非 `fail`。用例据此设计三个达人：粉丝 1200 + 近 2 天 1.2 万赞作品 → 两条规则都 pass → **pass**；粉丝 7000（超出 0–5000）→ follower-range fail 压倒一切 → **fail**；粉丝 1300 但作品只有 2000 赞 → followers pass、recent-post-likes unknown → **unknown**。三者都带粉丝数，可信度均为 1.0，不会误入跳过分支。
  **断言**：`sendBatch` 三次入队且 `followerCount` 依次为 `[1200, 7000, 1300]`（证明三种结论的结构化观测都照常进事实账本，闸门只挡证据）；`profilePage.screenshot` 恰好一次且参数为 `{ fullPage: false, quality: 75, type: 'jpeg' }`；`uploadScreenshot` 恰好一次，其 `observation.creatorObservationId` 等于 pass 那条观测的 id（按 `followerCount === 1200` 反查批次，不依赖调用顺序），`mimeType/purpose/runId` 与截图字节一并核对；`decorateRuns` 的 progress 为 `{ candidatesFound: 1, creatorProfilesSeen: 3, feedItemsSeen: 3 }`，且 `lowConfidenceDiagnostics` 为 `{ consecutiveFailures: 0, skippedTotal: 0 }` —— 最后这条把「unknown 来自硬筛」与「被低可信度跳过」区分开，否则用例可能在错误的原因下变绿。
  **变异验证**：把 `runtime.ts:509` 改回 `filter.outcome === 'fail'` 后用例失败于 `expect(profilePage.screenshot).toHaveBeenCalledOnce()`，实际 2 次（pass 与 unknown 都截了图），正是本组要消除的浪费；改回 `!== 'pass'` 后 4/4 全绿。
  **一个踩到的坑（记录以免重犯）**：mock 的 `getDevice` 若沿用邻近用例的 `deviceId: 'device-1'`，`collectorBatchSchema.parse` 会抛 ZodError —— `collector.ts:54` 要求 `deviceId: z.uuid()`。邻近用例不构造批次所以一直没暴露；本用例必须用合法 UUID。验证：`npm run test -w @douyin/collector` 10 文件 40 用例全绿，`npm run typecheck`、`npm run lint`、`npm run format:check` 全仓通过。
- [x] 6.3 不为这一行单独发版：确认服务端阶段 A-C 上线期间 v0.1.5 采集器完全可用，unknown 的多余截图由第 5 组回收；把该行改动排入下一次自然发包。验证：`artifacts/` 下不产生新的采集器包，`apps/collector/package.json` 版本号未变。**实施备注**：**兼容性论证**（不是推测，逐条对到了代码）：本组只改了采集端一个截图条件，`packages/contracts` 与 `apps/collector/package.json` 的 diff 为空（见 6.1），批次结构、`COLLECTOR_PROTOCOL_VERSION`（仍 `1.0.0`）与幂等键格式都没变，服务端 `assertCollectorProtocolCompatible` 只比对主版本号，因此已发布的 v0.1.5 上报的批次照旧被接受；`COLLECTOR_MIN_VERSION` 默认 `0.1.0`，v0.1.5 不会被版本门槛拒掉。阶段 A–C 上线后 v0.1.5 的行为差异只有一处：它对 `unknown` 达人仍会截图上传，而服务端闸门已不再为 `unknown` 建候选行，这些对象因此成为无候选行引用的孤儿 —— 正是第 5 组 `ORPHAN_MEDIA_CLEANUP_ENABLED` 回收的对象（宽限期默认 7 天，见 5.1–5.6），成本有界且自动收敛，不需要运维介入，也不会阻塞采集或产生错误。**发布安排**：该行随下一次自然发包一起出（届时按 `docs/collector-windows.md` / `docs/collector-macos.md` 的既有流程构建与真机验证），本次不单独构建、不单独发版。验证：`node -p "require('./apps/collector/package.json').version"` 输出 `0.1.5`（未变）；`ls artifacts/` 最新的仍是 `collector-windows-v0.1.4*`，本次没有产生任何新包；`git status --short` 中 `apps/collector` 下只有 `runtime.ts` 与 `runtime-settings.test.ts` 两个已跟踪文件被修改。

## 7. 文档同步

- [x] 7.1 更新 `docs/product-manual.md` 与 `docs/operations/operator-guide.md`：说明「证据不足」分区已取消及原因、达人库改为五个跟进阶段分区、人工复核通过后自动进入「待联系」、不通过自动进入「不合适」、以及未入库达人如何在运行详情中查看判定依据。验证：文档中的界面名称与 `apps/web/src/constants.ts` 的标签常量逐一对应，命令示例可完整复制且不含真实密码、Secret、Cookie、令牌或私钥。**实施备注**：`product-manual.md` 第 7 节新增「判定依据」小节（面板标题、副标题、每位达人的字段、总体结论与「已入库 · <阶段>」/「未入库 · 不进入复核队列」标注、「共 N 位 · 已入库 M 位」计数），第 8 节重写为 8.1 入库闸门 / 8.2 分区（六行表格，逐列对应 `candidateSections` 的 label 与 statuses）/ 8.3 复核（四条自动推进规则），第 9 节随之改为「通过复核后已自动处于待联系」并把可推进阶段收窄为「已联系、沟通中、已合作、不合作」。`operator-guide.md` 第 4 节重写为「哪些达人会进达人库 / 分区 / 复核与跟进」三小节，并在第 5 节异常处理里补一条「达人库里没有刚采集的达人」指向判定依据面板 —— 运营遇到这个现象时通常先翻异常处理，不会先读第 4 节。
  **界面名称逐一对应已完成**（脚本核对，23 个术语全部在 `apps/web/src` 有实际出处）：分区与阶段标签取自 `constants.ts` 的 `candidateSections`（全部/待复核/待联系/跟进中/已合作/不合适）与 `pipelineStatusOptions`（待复核/不符合/待联系/已联系/沟通中/已合作/不合作），复核结论取自 `reviewDecisionOptions`（人工通过/不符合/待定），硬筛结论与规则名取自 `lib/format.ts` 的 `hardFilterLabel`（通过/不通过/未知）与 `ruleLabel`（粉丝范围/近期爆款作品），「我负责的」取自 `TodayPage.tsx:97`，「运行监控」「达人库」取自 `OperationsDesk.tsx:220,228`，「本次运行观察到的达人」及两条入库标注取自 `RunsPage.tsx:195,221`。**顺带修正一处既有的名称漂移**：原文写「提交『通过、不符合或待定』」，而 `reviewDecisionOptions` 的标签是「人工通过」，已按常量改正。
  **自动推进的四条规则逐条对到了实现**（`candidate-workflow.ts:148-200`）：`advancedStatus` 仅在 `pipeline_status === 'pending_review' && decision !== 'pending'` 时非空，`approved → to_contact`、否则 `unsuitable`；`pipeline_status_changed` 事件只在 `advancedStatus` 非空时追加，`manual_reviewed` 总是追加；`version = version + 1` 只在那一条 UPDATE 里发生，因此文档写「每次成功提交只递增一次候选版本」。**判定依据为重算而非快照**同样核对过：`run-observed-creators.ts:77` 读 `versions.rules_json`（该运行钉住的规则版本），:121 在读时调用 `evaluateHardFilters`，:131 的 `admitted` 取决于候选行是否存在 —— 所以文档可以断言「之后修改任务规则不会改变历史运行的判定依据」。命令示例：本次新增内容不含任何命令，只有界面名称、表格与日志字段示例，因此「可完整复制且不含秘密」自然满足；另用正则扫描四个改动文档的 `password|secret|token|cookie|api_key|私钥|密码` 赋值形式，无命中。
- [x] 7.2 更新 `docs/operations/admin-guide.md`：说明 `hardFilterStatus` 服务端过滤仍可用但默认界面无入口、孤儿回收开关与宽限期天数的含义、以及回收是唯一不可逆操作因此默认关闭。验证：新增变量名与 5.1/5.2 的实现完全一致。**实施备注**：新增第 4 节「入库闸门与证据回收」，原「管理交接」顺延为第 5 节。`hardFilterStatus` 部分写明界面无入口但 `GET /candidates`（`server.ts:767,793`）与 `GET /exports/candidates.csv`（`server.ts:833,855`）仍接受该参数、取值 `pass|fail|unknown`（`candidate-library.ts:39`）、用途是检索闸门上线前的存量行、且同样受工作区与角色鉴权约束。孤儿回收部分用三行表格把三条清理路径的判据与计数字段并列，避免读者误以为新路径与既有路径等价。
  **对「唯一不可逆」的表述做了修正**：初稿写成「其他路径针对从未被业务引用的临时对象」，核对 `media-cleanup.ts:64-86` 后发现不成立 —— 留存路径删的正是 `confirmed` 素材，只是要求 `confirmed_at` 超过 180 天且所属任务已归档或不存在。改为按风险差异表述：前两条要么针对从未确认成功的临时对象，要么要求任务已归档且证据满 180 天；孤儿路径却会在任务仍活跃、证据只有 7 天大小时删除已确认截图，判据只有「没有候选行引用」一条，且 OSS 对象删除后不可恢复、主页也无法重新采集。这才是它默认关闭的真实理由。
  **另补了两条排查时可依赖的保守约束**（都来自 `selectOrphanRows`，:111-137）：素材必须能通过 `creator_observation_id` 内连接到真实的达人观察记录，因此该列为空或只关联作品观察的素材一律不回收；判据粒度是达人而非单条观察（`candidates.creator_id = creator.creator_id`），只要该达人在本工作区有任意一条候选行，其名下所有素材都保留。批量上限与「一次开启不会删穿整张表」的行为指向 5.5 的行为测试。
  **变量名与实现完全一致**（逐个核对 `config.ts` / `compose.yml` / `preflight-production.mjs` / `.env.production.example` / 本节文档五处）：`ORPHAN_MEDIA_CLEANUP_ENABLED`（默认 `false`，`booleanFlag`，预检限定 `true/false/1/0`）、`ORPHAN_MEDIA_GRACE_DAYS`（默认 7，`int().min(1).max(3650)`）；同时核对了既有的 `MEDIA_RETENTION_DAYS=180`、`MEDIA_CLEANUP_BATCH_SIZE=100`（上限 500）、`MEDIA_CLEANUP_INTERVAL_MS=3600000`（文档写「默认 1 小时」）。日志字段名与顺序取自 `MediaCleanupSummary` 的实际展开顺序，与 5.6 本地实跑输出一致。
  **超出任务字面范围但按 AGENTS.md 要求同步的一处**：`docs/operations/production-deployment.md` 的「日常检查」补两条 —— 迁移 `0014_media_cleanup_index.sql` 随本轮前向执行且为在线 DDL，以及开启孤儿回收前的前置确认与「先关着部署、观察一轮再开」的建议顺序。理由是该文件的「首次准备」只让运维复制 `.env.production.example` 而不逐项列举变量（两个新变量连同解释性注释已在 5.x 写入该示例文件），因此变量说明落在管理员手册，而**发布顺序与迁移事实**属于部署文档，两边不重复。
- [x] 7.3 更新 `README.md` 中涉及达人库分区或复核流程的描述（如有）。验证：`grep -n "证据不足\|待补证据" README.md docs` 无遗留旧说法。**实施备注**：README 原本没有分区描述，只有「能力概览」两条相关条目，已就地扩写为「只有全部硬筛通过的达人才入库成为候选并采集截图，未入库达人的判定依据仍可在运行详情中查看」与「复核结论自动推进合作阶段，达人库按合作阶段分区」，未新增章节（README 保持概览定位，细节留在产品手册）。
  **该验证命令无法字面通过，属有意偏离**：`grep -rn "证据不足\|待补证据" README.md docs` 命中 2 处，分别是 `docs/product-manual.md:163` 与 `docs/operations/operator-guide.md:41`，两句都是 7.1 要求写的「原来的『证据不足』分区已取消」及其原因说明。7.1 要求说明该分区已取消，就必须出现这个名称；7.3 的 grep 又要求该名称不出现 —— 两条任务在字面上互斥。已确认全部命中都属于「说明其已取消」的语境，没有任何一处仍把它描述为现存分区，即该 grep 想防的「忘记更新文档」并未发生。**选择保留术语而不是改写文档去迎合 grep**：运营同事记得旧界面上的名字，说明取消时必须用旧名才能对上；改成「原来那个表示硬筛未通过的分区」反而更难懂。除此之外全仓已无其他遗留说法（`docs/local-development.md:45` 的「按日期筛选和分组」为中性表述，仍然成立；`docs/collector-*.md` 不涉及分区，且按 6.3 本次不发版，其版本示例保持在 0.1.4）。

## 8. 全量验证与分阶段上线

- [x] 8.1 运行 `npm run check`，确认 format、lint、typecheck、单测与构建全绿。验证：命令退出码为 0。**实施备注**：`check` 展开为 `format:check && lint && typecheck && test && build` 五段串行，退出码 0，因此五段全部通过。构建产物正常（`@douyin/web` vite 输出 `index.html` 0.46 kB、CSS 34.91 kB、JS 282.73 kB / gzip 87.07 kB；那条「chunk 大于 500 kB」的提示是既有告警，与本次改动无关，实际 JS 也只有 282 kB）。
- [x] 8.2 运行 `npm run test:mysql` 与 `npm run test:e2e`，确认集成与端到端全绿。验证：两条命令退出码为 0（需本机 Docker 与 Chrome）。**实施备注**：本机 Docker 28.0.4 在运行，两条命令退出码均为 0。`npm run test:mysql` 走 `scripts/run-mysql-integration.mjs`，该脚本对每个阶段 fail-fast（`run()` 在非零退出时抛错），因此退出码 0 即代表 domain 集成、api 集成（8 文件 16 用例）、`local-flow.e2e.spec.ts`（1 passed / 30.4s）三段全绿；脚本 `finally` 已 `down --volumes --remove-orphans`，容器与网络均已清理，不残留 33307 测试库。`npm run test:e2e` 共 21 条：20 passed、1 skipped。
  **那 1 条 skip 需要说明，它不是本次改动造成的**：跳过的是 `local-flow.e2e.spec.ts`，其 :32 有既有条件 `test.skip(!process.env.MYSQL_TEST_URL, ...)`；`playwright.config.ts` 的 `webServer` 只起 web 预览服务（127.0.0.1:4187），不注入 `MYSQL_TEST_URL`，所以单独跑 `test:e2e` 时它必然跳过，而该用例已在上一段 `test:mysql` 里带真实 MySQL 实际跑通并通过。`git diff -- tests/` 中不含任何 `skip`/`fixme` 的增删，可确认与本组无关。
- [x] 8.3 运行 `openspec validate "tighten-review-funnel" --strict`，确认实施后 delta specs 仍与实现一致。验证：输出 `Change 'tighten-review-funnel' is valid`。**实施备注**：输出正是 `Change 'tighten-review-funnel' is valid`，退出码 0，在第 1–7 组全部实施完成后执行，5 个 delta spec（`creator-candidate-library`、`creator-review-and-outreach`、`douyin-local-collection`、`hosted-platform-operations`、`screening-campaigns`）与实现一致。
- [x] 8.4 按 design.md 的 Migration Plan 依次上线阶段 A（api + worker 镜像）与阶段 B（web 镜像），沿用本地构建 + `docker save` tar 侧载流程与 `YYYYMMDD-N` 标签，切换前保留 `.previous-images.env` 与 infra 备份。验证：`/health/ready` 返回健康，阶段 A 后观察一次真实运行的候选行创建量符合预期。**上线记录（2026-09-22，tag `20260921-1`，镜像 revision `ceef9bd`）**：本机 build + `docker save` 侧载 122MB tar，服务器 `sha256sum -c` OK；`.previous-images.env` 已从 `20260916-1` 覆盖为 `20260920-1`，并另存 `compose.yml.prev-20260920`；`compose.yml` 的 diff 确认只新增两行 `ORPHAN_*`（现网未被手工改过）。发布前跑了 `scripts/test-production-images.ps1`：smoke compose 用 tmpfs MySQL 真跑一遍 migrate 后四个容器全部 healthy，grype 三个镜像均 `No vulnerabilities found`。迁移输出 `{"status":"ok","applied":["0014_media_cleanup_index.sql"],"skipped":[0001…0013 共 13 项]}`。切换后容器内 `/health/ready` 返回 200、`components` 只有 `mysql`+`oss` 且都 up、`status:"ok"`；`ps` 显示 api/worker/web 均为 `20260921-1`；worker 首轮清理日志 `deletedOrphans:0`、`failed:0`，证明阶段 C 未被误触发；api 日志无 error/fatal；管理后台界面已人工核对无异常。**两处 runbook 纠错已记入项目记忆**：`compose.yml` 用 `${API_IMAGE}` 插值，切镜像必须同时 sed `.env.production` 的三行 tag，否则 `up` 会原地重建旧版本；`nginx.conf.template` 用裸 `proxy_pass http://api:3000` 且无 `resolver`，nginx 只在加载配置时解析一次，api 换 IP 后必须 `--force-recreate gateway`，否则网关侧持续 502。**验收子句后半段已补齐（2026-09-22 真实运行）**：运营用现网 v0.1.5 采集器跑完「校园素人爆款」一次真实采集，服务端记录 `feedItemsSeen` 100 / `creatorProfilesSeen` 96 / `candidatesFound` 1（本机助手控制页显示为 浏览作品 100 / 核验作者 96 / 硬筛通过 1）、解析跳过 0，停止原因 `max_feed_items`（`determineRunStopReason` 按 targetCandidates→maxFeedItems→maxCreatorProfiles→maxDurationMinutes 顺序判定：硬筛通过 1 < 目标候选数 30 未命中，浏览作品 100 ≥ 最多浏览作品 100 命中；「筛选任务」编辑器不提交 `maxCreatorProfiles`，故该条恒被跳过，这也是核验作者能到 96 而非被默认 50 卡住的原因）。**候选行创建量符合预期**：96 位核验主页只产生 1 条候选行，换成本次发布前的服务端行为会建 96 行，正是 D1 闸门要消除的浪费；解析跳过 0 说明那 95 位不是因低可信度被丢弃，而是硬筛判为 fail/unknown。**但本次截图上传量并未下降，这是预期行为，必须写清以免误判 8.5**：组 6 的「只有硬筛通过才截图」改的是 `apps/collector/src/runtime.ts:509`（判据由 `filter.outcome === 'fail'` 收紧为 `!== 'pass'`，commit `3a90ed7`），属采集端代码、**不在服务端镜像里**；按 design.md 阶段 D 它搭下一次自然发包，本次没有发新包，运营电脑上跑的仍是 v0.1.5 —— 其判据是「fail 不截图、**unknown 照截**」。而服务端 `issueMediaUpload` 只经 `assertUploadScope` 校验观测归属于该运行与该设备、**不看入库资格**，所以本次运行仍会为 unknown 达人写下 `media_objects` 行。这些行没有候选行消费者，正是 8.5 孤儿回收的目标：`reclaimable` 基线不会是 0，且这批对象的 `confirmed_at` 为 2026-09-22，需满 7 天宽限期才进入回收窗口，与 8.5 的目标启用日天然吻合；等阶段 D 随下一次发包落地后，新产生的孤儿才会归零。该运行已由用户在管理后台「运行监控」人工核对（含「本次运行观察到的达人」判定依据面板），反馈无异常。
- [ ] 8.5 观察期结束（确认候选行创建正常、复核流转正常、无运营反馈数据缺失）后再显式开启阶段 C 的孤儿回收开关，以 7 天宽限期运行一轮并核对回收数量。验证：回收日志中的对象数量与预期一致，且 `media_objects` 表行数未减少（仅状态变化）。**观察期自 2026-09-22 起算，目标启用日约 2026-09-29**：阶段 A/B 已上线且首次真实运行符合预期（见 8.4：96 位核验主页只建 1 条候选行）。启用前需同时满足四个信号 —— ① 该次运行的 1 位入库达人经人工复核后自动进入「待联系」分区（组 2 的自动流转在真实数据上生效）；② 期间再有真实运行且候选行创建量继续符合预期；③ 无运营反馈「达人库少了人」；④ 启用**前**先在 DMS 记录两个只读基线数：`SELECT COUNT(*) AS total_rows FROM media_objects;`（阶段 C 跑完后必须**完全相同** —— `deleteRows` 只把状态改成 `expired`→`deleted`，从不删行，这正是本条验证方法的依据）以及与 `selectOrphanRows`（`packages/domain/src/media/media-cleanup.ts:111-137`）逐字对应的可回收数：`SELECT COUNT(*) AS reclaimable FROM media_objects media JOIN creator_observations creator ON creator.workspace_id = media.workspace_id AND creator.id = media.creator_observation_id WHERE media.status = 'confirmed' AND media.confirmed_at <= NOW(3) - INTERVAL 7 DAY AND NOT EXISTS (SELECT 1 FROM campaign_candidates candidates WHERE candidates.workspace_id = media.workspace_id AND candidates.creator_id = creator.creator_id);` —— 该数应与启用后 worker 日志里 `deletedOrphans` 的累计值一致。现网数据都远新于 180 天，留存路径命中恒为 0，不会与孤儿路径争抢同一行。**注意基线不是静态值**：运营端仍是 v0.1.5、每次运行都会继续为 unknown 达人产生孤儿（见 8.4 的取证说明），所以 `reclaimable` 会随新运行增长，`deletedOrphans` 的累计值应对齐**启用时刻**取到的基线数；真正的止血要等阶段 D 随下一次发包落地。**启用动作只涉及 worker**：改 `.env.production` 两行（`ORPHAN_MEDIA_CLEANUP_ENABLED=true`、`ORPHAN_MEDIA_GRACE_DAYS=7`）后 `docker compose --env-file .env.production -f infra/production/compose.yml up --detach --no-deps --force-recreate worker`，worker 不是 nginx upstream，**不需要**重建 gateway；`apps/worker/src/index.ts:62` 启动即跑一轮清理，随后每小时一轮、每轮每条路径上限 `MEDIA_CLEANUP_BATCH_SIZE`=100 个对象，因此基线数较大时需数小时才追平，`failed` 必须为 0。回滚：把开关改回 `false` 并再次 recreate worker 即停止回收，但**已删除的 OSS 对象不可恢复**，这是全流程唯一不可逆步骤。
- [x] 8.6 全过程不提高 `COLLECTOR_MIN_VERSION`。验证：上线后 v0.1.5 采集器可正常 claim 运行、同步批次与上传截图，无任何升级提示。**代码侧确认（2026-09-22）**：`git diff c8b47d8..HEAD` 对 `COLLECTOR_MIN_VERSION` 零命中（唯一命中是 tasks.md 里 6.1/6.3 备注的文本），仓库三处取值全部未变 —— `.env.production.example:28` 为 `0.1.0`、`infra/production/compose.yml:90` 默认 `0.1.0`、`packages/domain/src/config.ts:39` 的 schema 未改；`apps/collector/package.json` 仍为 `0.1.5`，`artifacts/` 未产生新包。发布本身也不要求采集器更新：批次结构、`COLLECTOR_PROTOCOL_VERSION`（`1.0.0`）与幂等键格式均未变，服务端 `assertCollectorProtocolCompatible` 只比对主版本号。**现网运行验证已补齐（2026-09-22）**：8.4 记录的那次「校园素人爆款」真实运行即本条验收证据 —— 运营电脑上现有的 v0.1.5 采集器成功 claim 该运行、按轮上报进度并同步批次，服务端据此算出停止原因并把运行置为 `completed`，助手显示「已达到停止条件。请到达人库复核本次结果。」（`apps/collector/src/runtime.ts:333`），全程没有任何升级提示；入库的那 1 位达人已由用户在管理后台核对可见。**这比读取服务器环境变量更强**：`authorizeCollectorRequest`（`apps/api/src/server.ts:179-193`）对**每一个**采集器请求都执行 `compareSemanticVersions(effectiveCollectorVersion, collectorMinVersion) < 0 → 426 COLLECTOR_UPGRADE_REQUIRED`，若现网 `COLLECTOR_MIN_VERSION` 被抬到 0.1.5 以上，claim、syncRuns、reportProgress 与批次上传会全部 426，运行根本推进不到 100 浏览作品；该运行完整跑通即证明现网闸门仍接受 v0.1.5，无需再单独确认该值。**取证范围说明**：claim、进度上报与批次同步三条链路都有服务端侧的直接证据（运行状态、进度计数与停止原因均由服务端算出并回给采集器）；「上传截图」这一条走的是同一个 `authorizeCollectorRequest`，因此版本闸门同样不会拦它，但截图直传本身是否成功属 OSS 链路、本次未单独取证 —— 目前由 `/health/ready` 的 `oss: up` 与用户在管理后台的核对覆盖。若要一次性坐实，打开达人库里那 1 位待复核达人的主页截图即可；反之若签名上传失败，助手会在控制页顶部持续显示「有待同步数据 / 截图保存在本机」（`apps/collector/src/control-page.ts:107`）并保留本机证据，不会静默丢弃。
